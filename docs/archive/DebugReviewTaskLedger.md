# Debug.txt Review Task Ledger

**Source reviewed:** `C:\Users\D2O\Desktop\Debug.txt`  
**Source label:** `Beta0.4.2`  
**Repository baseline source:** `FUNCTION_MAP.md`, `README.md`, `Architecture/Beta0.4.2ShortTermPlan.md`, `Architecture/RepositoryOptimizationAudit.md`, and discovered `tests/`, `core/tui/`, `permission/`, `plugins/`, `agents/`, and `core/llm*` paths.  
**Scope:** documentation-only triage ledger. No source behavior, tests, runtime configuration, or implementation files are changed by this artifact.

**BUG prioritization and minimal fix planning:** see `Architecture/DebugBugFixPlan.md` for the blocker/severe/normal ordering, per-BUG minimal repair boundary, candidate source/test mapping, acceptance commands, user-visible behavior notes, confirmation requirements, and non-BUG rationale.

## User-Confirmed Constraints to Preserve

- TUI visual direction: Opencode-like minimal terminal style.
- Logo direction: minimal text logo, theme-appropriate, not an image-heavy or complex mark.
- Documentation language split: `README.md` must be English; `README.zh-CN.md` must contain Chinese documentation if/when created. The current `README.md` contains mixed English and Chinese sections, and `README.zh-CN.md` is not present.
- Opencode alignment target: imitate Opencode as comprehensively as possible across directory structure, TUI, permission model, Agent model, and plugin mechanism, while retaining SuperMedicine project identity and legal/source-code boundaries.
- Refactor boundary: only merge genuinely duplicated logic. Dead code may be deleted only when there is evidence that it is unused and safe to remove. No functionality may be changed by a refactor unless a later implementation task explicitly authorizes that change.

## Task Ledger

Legend for execution status:

- **Directly executable:** enough evidence exists to start implementation/design work without additional user decision.
- **Needs confirmation:** user-visible product or policy choice must be confirmed before implementation.
- **Evidence first:** investigation must produce repository/runtime evidence before any source change is authorized.

## Step 1 Current Debug.txt Triage Ledger

**Review date:** 2026-06-20
**Purpose:** Archive every current actionable entry from `C:\Users\D2O\Desktop\Debug.txt` exactly once as pending/triage work. This section supersedes older Step 1 matrices for the current Debug.txt source; it records scope and module mapping only, and does not authorize root-cause fixes.

| Current ID | Debug.txt line | Category | Pending / triage requirement | Concrete owner files / classes / functions | Exact repair-location candidates | Verification commands / tests / manual checks | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CUR-DBG-001 | 4 | BUG | Merge all log output into one log file/container per application opening, with every category logged as newline-separated records until thread/application exit. | `core.log_report.LogReportStore.write()`, `append()`, `_write_isolated()`; `core.log_report_handler.LogReportLoggingHandler.emit()`, `configure_tui_log_storage()`, `append_tui_stream_output()`; `core.log_report_models.TUI_LOG_SESSION_ID`, `resolve_log_storage_locations()`; `core.tui.app.launch_tui()`; `cli.logging_setup._configure_cli_logging()`; `gui_entry.setup_logging_for_gui()`; `gui_standalone.setup_gui_logging()`; `core.tui.screens.log_screen.LogReportView.refresh_logs()`. | If one file per launch is required rather than the current stable TUI session file, add a launch-scoped session ID in `core/log_report_models.py` and thread it through `configure_tui_log_storage()` and `launch_tui()`. Route GUI `gui.log` / `gui_standalone.log` stdout/stderr into `LogReportStore` instead of separate files. Ensure `LogReportLoggingHandler.emit()` appends category/source/module as records in one session file. | Run targeted checks: `python -m pytest tests/test_log_report.py tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: launch TUI/GUI, trigger chat/tool/error logs, inspect `.supermedicine/logs/` and confirm all records for the opening are in the intended single log container. | Pending triage |
| CUR-DBG-002 | 6 | BUG | Fix R/Python tool recognition/import so heatmap and UMAP scan correctly; tools must remain globally visible and scannable after workspace selection. | `core.workspace_tools.WorkspaceToolService.tool_source_root()`, `scan_import_candidates()`, `_candidate_from_source()`, `_resolve_candidate_selections()`, `import_scanned_tools()`, `_import_candidate()`, `list_tools()`; `cli.commands.tool.tool_scan()`, `tool_add()`, `tool_list()`; `core.tui.screens.tool_screen.ToolView._scan_candidates()`, `_add_selected_tool()`, `_load_tools()`; `core.web.server.tool_scan()`, `tool_create()`, `tool_list()`; `core/web/frontend/app.js::scanTools()`, `renderScanResults()`, `addScannedTools()`, `loadTools()`; manifests `plugins/tools/heatmap/tool.yaml`, `umap/tool.yaml`, `r_heatmap/tool.yaml`, `r_umap/tool.yaml`. | Fix frozen/source root fallback in `WorkspaceToolService.tool_source_root()` if `Path.cwd()` is not repository root. Keep `tool.yaml` recognition in `_candidate_from_source()` for heatmap/UMAP/R variants. Repair GUI mismatch: `/api/v1/tools/scan` returns grouped dict but `app.js::scanTools()` expects array; flatten grouped results or change API. Use scan candidate `index` or `language/id` as checkbox value instead of `name` in `renderScanResults()`. Flatten grouped `tool_list()` for GUI or render grouped dict. | Run: `python -m pytest tests/test_workspace_full.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual CLI: `supermedicine tool scan`, then `supermedicine workspace init --workspace <id>`, `supermedicine tool add --workspace <id> --select python/umap`, `supermedicine tool list --workspace <id>`. Manual GUI: choose workspace, scan, add heatmap/UMAP, refresh list. | Pending triage |
| CUR-DBG-003 | 8 | BUG | Fix streaming Stream animation/output text display bounds so streamed text uses the full chat dialog instead of a collapsed/deformed region. | `core.tui.screens.chat_view.ChatView.begin_assistant_message()`, `append_assistant_delta()`, `compose()`; `core.tui.app.SuperMedicineTUI._run_kernel_task()` / inner `render_progress()`; CSS selectors `#chat-output`, `#thinking-indicator`, `#processing-indicator` in `core/tui/app.tcss`; web `core/web/frontend/app.js` WebSocket `ws.onmessage` `progress`/`result`; CSS `#messages`, `.message`, `.message.assistant`, `.message.progress` in `core/web/frontend/style.css`. | Replace per-delta `RichLog.write()` narrowing with an accumulated full-width assistant streaming block in `ChatView.append_assistant_delta()` / `begin_assistant_message()`. Ensure `#chat-output` remains `width: 100%` and indicators do not constrain it. For web, avoid rendering streaming chunks as separate narrow `.message.progress` bubbles; add/maintain a full-width active assistant message if streaming is enabled. | Run: `python -m pytest tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual TUI/GUI: trigger streaming response and confirm streamed text occupies full chat dialog width without deformation. | Pending triage |
| CUR-DBG-004 | 10 | BUG | Make LLM thinking animation visible and fixed inside the chat dialog at the lower-right corner. | `core.tui.screens.chat_view.ChatView.start_thinking_animation()`, `_advance_thinking_frame()`, `stop_thinking_animation()`, `append_thinking_content()`; `core.tui.app.SuperMedicineTUI._run_kernel_task()` / `render_progress()` handling `reasoning`, `thinking_content`, `thinking_done`; CSS selector `#thinking-indicator` in `core/tui/app.tcss`; LLM progress source `core.kernel_llm_chat` / `core.kernel.Kernel.execute_task(progress_callback=...)`. | Align emitted progress event kinds with `render_progress()` handling. Reposition `#thinking-indicator` from current overlay/dock rules to fixed lower-right inside chat output. If CSS cannot anchor inside `RichLog`, wrap chat output/indicator in a chat container in `ChatView.compose()` and anchor there. Add equivalent GUI thinking element if EXE GUI parity is required. | Run: `python -m pytest tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: trigger an LLM reasoning/progress event and confirm the thinking animation is visible inside the chat dialog at lower-right. | Pending triage |
| CUR-DBG-005 | 12 | BUG | Rename the “对话处理中” animation text to `Processing` and place it outside the chat dialog at the input box’s outer lower-left corner without adding an extra frame. | `core.tui.i18n.LABELS['chat_processing_state']`, `LABELS['input_placeholder_processing']`; `core.tui.screens.chat_view.ChatView.start_processing_animation()`, `_advance_processing_frame()`, `stop_processing_animation()`; `core.tui.app.SuperMedicineTUI._set_chat_processing()`, `_update_prompt_input_lock()`, `compose()`; CSS selector `#processing-indicator`, `#input-bar`, `#chat-output` in `core/tui/app.tcss`. | Change only animation display copy to `Processing` if status-bar Chinese text should remain localized. Move processing indicator out of `ChatView` overlay or add a dedicated `Static` near `#input-bar` in `SuperMedicineTUI.compose()` so it appears outside chat dialog/input lower-left. Remove `border`/extra frame/background from `#processing-indicator` CSS. Keep cleanup calls idempotent. | Run: `python -m pytest tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: submit chat; verify `Processing` appears outside chat dialog at the input area lower-left without a border/frame. | Pending triage |
| CUR-DBG-006 | 14 | BUG | Prevent experiment guide and permission-related selectable regions, including experiment ID selection and field-description areas, from enlarging when focused/selected. | CSS `DataTable:focus`, `#experiment-protocol-table`, `#experiment-protocol-table:focus`, `#experiment-input-table`, `#experiment-input-table:focus`, `#permission-roots-table`, `#permission-roots-table:focus`, `#experiment-data-input` in `core/tui/app.tcss`; `core.tui.screens.experiment_screen.ExperimentGuideView.compose()`, `refresh_session_view()`; `core.tui.screens.permission_screen.PermissionView.compose()`. | Remove or narrow global `DataTable:focus { max-height: 50%; }`. Add focused-state height/max-height parity for affected selectors. Confirm `TextArea`/field description surfaces such as `#experiment-data-input` retain the same size focused/unfocused. Audit other `DataTable` screens for the same global focus enlargement. | Run broad TUI checks: `python -m pytest tests/test_tui.py tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: focus/unfocus experiment protocol table, input field table, permission roots table and confirm dimensions remain unchanged. | Pending triage |
| CUR-DBG-007 | 16 | BUG | Remove the stale upper-left TUI old-menu shortcut/hollow-circle residue and retain only the current new menu shortcut button. | `core.tui.app.SuperMedicineTUI.compose()` yielding `Header()`, custom `MenuButton`, `Footer()`; `SuperMedicineTUI.BINDINGS`; `SuperMedicineTUI.action_command_palette()`; `core.tui.nav_widgets.MenuButton.on_click()`; `core/tui/app.tcss` selectors `Header`, `Footer`, `#menu-button`, `.menu-affordance`; `core.tui.menu_screens.MainMenuScreen`. | Identify whether hollow circle comes from Textual `Header`, `Footer`, command palette, or binding footer. Hide/remove `Footer()` or selected binding display if it renders stale shortcut. Replace or style Textual `Header` to suppress the old affordance. Preserve only custom `MenuButton("≡ ...", id="menu-button")`. | Run: `python -m pytest tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual visual check: launch TUI and confirm upper-left only has the `≡` menu button and no hollow-circle old shortcut. | Pending triage |
| CUR-DBG-008 | 18 | BUG | Fix experiment guide double-click field behavior so only a selected field can be pasted to the terminal/input and switching fields does not paste the previously selected field. | `core.tui.screens.experiment_screen.ExperimentGuideView.on_data_table_row_selected()`, `_paste_selected_field()`, `_paste_field_name()`, `refresh_session_view()`, `_switch_protocol()`; widget `DataTable(id="experiment-input-table", cursor_type="row")`; paste target `TextArea(id="experiment-data-input")`. | Separate single-row selection from paste action. Track last selected row/activation count or use Textual event metadata so switching rows never calls `_paste_field_name()` for the old row. Restrict paste to explicit paste button or true double activation of the already-selected row. Clear tracked row state on session/protocol refresh. | Add/run focused TUI test in `tests/test_tui.py` or a new experiment screen test; command: `python -m pytest tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: select field A, switch to B, verify no paste; double-click selected B and verify only B is pasted once. | Pending triage |
| CUR-DBG-009 | 20 | BUG | Localize the EXE GUI, ensure only the Chat page has a chat dialog, and align GUI behavior with TUI and CLI capabilities. | GUI wrappers `gui_entry.main()`, `gui_standalone.main()`, `start_web_server()`; web API `core.web.server.create_app()` endpoints `/api/v1/*` and `/ws/chat`; frontend `core/web/frontend/index.html` tab sections and chat form; `core/web/frontend/app.js` `initTabs()`, `loadTabData()`, all setup/render functions; `core/web/frontend/style.css` `.tab-content`, `#tab-chat`, `#messages`, `#chat-form`; parity references `cli.parser.main()`, `cli_entry.CLI`, `core.tui.app.SuperMedicineTUI.NAV_ITEMS`. | Localize remaining English/hybrid GUI strings. Ensure `#messages` and `#chat-form` exist and display only under `#tab-chat`; repair `.tab-content` / `#tab-chat.active` CSS if chat leaks into other pages. Align GUI values/API calls with CLI: permission modes `sandbox/conservative/full`, experience scope `general/workspace`, tool grouped responses, LLM add/list/switch/show. Audit all tab actions against CLI/TUI capabilities. | Run web tests: `python -m pytest tests/test_web_self_evolution.py tests/test_web_self_evolution.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual EXE/GUI: open every tab, confirm Chinese/localized UI, chat dialog appears only on Chat, core actions match CLI/TUI. | Pending triage |
| CUR-DBG-010 | 22 | BUG | Fix the released desktop EXE icon so the new logo is reflected externally on the desktop shortcut/file, not only inside the EXE UI. | Assets `assets/logo.ico`, `assets/logo.svg`, `assets/logo.jpg`; CI PyInstaller commands in `.github/workflows/ci.yml` release app, GUI, installer steps; `scripts.ci.build_gui_exe.main()` command list; `scripts.ci.build_installer_exe.main()` command list; `scripts.ci.build_release_zip.main()` staging/copy; `installer.exe_release.release_exe_to_desktop()`; `gui_standalone.main()` pywebview `icon`. | Add/verify `--icon=<root>/assets/logo.ico` for release `SuperMedicine.exe`, GUI EXE, and installer EXE PyInstaller paths. Ensure `assets/logo.ico` is included in release payload/zip. If Windows icon cache masks changes, document overwrite/versioned filename behavior in `installer/exe_release.py`; executable resource icon still must be set at build time. | Run release/source checks: `python -m pytest tests/test_release.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: build EXEs, inspect Windows file properties/icon, release to temp desktop with `install_entry.py --release-exe dist/SuperMedicine.exe --desktop-dir <tmp> --exe-dry-run` or real copy and confirm visible desktop icon. | Pending triage |
| CUR-DBG-011 | 24 | BUG | Fix Chat workspace selection because the current Chat workspace bug breaks the workspace module. | Frontend `core/web/frontend/index.html` `select#chat-ws-select`; `core/web/frontend/app.js::loadChatWorkspaceSelector()`, `loadWorkspaceSelectors()`, `sendMessage()`; REST `core.web.server.chat()`; WebSocket `core.web.server.websocket_chat()` inner `_stream_result()`; CLI workspace path `cli_entry.CLI.run(workspace=...)`; workspace owner `core.workspace.WorkspaceManager`; TUI refresh `core.tui.app.SuperMedicineTUI.refresh_workspace_views()`. | In REST `/api/v1/chat`, read `workspace_id` and execute through `CLI.run(... workspace=workspace_id)` or inject the same workspace context into `Kernel.execute_task()`. In `/ws/chat`, parse and propagate `workspace_id`; current worker lambda only passes `message`. Ensure chat workspace selector loads on initial app load and after workspace create/delete. Avoid relying on `currentWorkspaceId` that is only updated by tool selector. | Add/run web API test for workspace propagation; existing command anchor: `python -m pytest tests/test_web_self_evolution.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual GUI: create workspace, select it in Chat, send message, confirm backend/log/result includes selected workspace context. | Pending triage |
| CUR-DBG-012 | 26 | BUG | Add scrolling support so TUI remains usable and fully visible in small-window mode. | Layout `core.tui.app.SuperMedicineTUI.compose()` containers `#app-body`, `#sidebar`, `#main-area`, `#content-pane`, `#input-bar`, `#status-bar`; CSS in `core/tui/app.tcss` for those selectors plus `#chat-output`, table selectors, form rows; screen classes under `core/tui/screens/*.py`; `core.tui.prompt_input.PromptInput`. | Ensure `#content-pane` and complex screen bodies are scrollable (`overflow-y: auto`) with bounded heights in small terminals. Replace fixed/min heights that exceed small windows. Wrap large screen contents in `ScrollableContainer` if Textual CSS alone is insufficient. Ensure focus can reach scrollable containers and mouse wheel works. | Run TUI tests: `python -m pytest tests/test_tui.py tests/test_tui.py tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: launch TUI in a small terminal, navigate all views, scroll to hidden controls. | Pending triage |
| CUR-DBG-013 | 28 | BUG | Remove all banned incremental-return hint text from the entire project frontend. | Text sources `core/tui/screens/chat_view.py::begin_assistant_message()` hardcoded streaming placeholder; `core/tui/i18n.py` chat/progress labels; `core/tui/app.py` chat result/status formatting; web text in `core/web/frontend/app.js`, `index.html`, `style.css`; repo hygiene owner `tests/test_repo_hygiene.py`. | Add/keep a static guard in `tests/test_repo_hygiene.py` or equivalent to prevent reintroduction. Remove the related assistant-generating placeholder in `ChatView.begin_assistant_message()`. Review web progress rendering so no model-returning-incrementally hint appears. | Run/static command: `python -m pytest tests/test_repo_hygiene.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual/static: repository-wide search for the banned incremental-return hint and related variants in source and built frontend assets. | Pending triage |
| CUR-DBG-014 | 30 | BUG | Ensure the current Processing animation is stopped/removed after Chat errors. | TUI lifecycle `core.tui.app.SuperMedicineTUI._run_kernel_task()` `try/except/finally`, `_set_chat_processing()`, `_update_prompt_input_lock()`; visual cleanup `core.tui.screens.chat_view.ChatView.stop_processing_animation()`, `stop_thinking_animation()`, `add_error_message()`; web error paths `core/web/frontend/app.js` WebSocket `ws.onerror`, `ws.onclose`, `ws.onmessage` case `error`, REST `.catch()` in `sendMessage()`. | TUI already calls `stop_processing_animation()` in `finally`; add `stop_thinking_animation()` and guard cleanup against exceptions so `_processing_active` and `_chat_processing` always reset. If GUI adds Processing state, clear it in WebSocket error/result and REST catch/finally paths and re-enable input/send button. | Run: `python -m pytest tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: trigger invalid provider/API error and confirm Processing/thinking indicators disappear and input is usable. | Pending triage |
| CUR-DBG-015 | 33 | Better | Move the GUI window menu items such as Dashboard and Chat to the left, collect them in a `≡` menu, and add smooth open/close transition animation. | Frontend markup `core/web/frontend/index.html` `#hamburger-btn`, `#drawer-menu`, `#drawer-overlay`, `.drawer-nav .tab-btn`; JS `core/web/frontend/app.js::openDrawer()`, `closeDrawer()`, `toggleDrawer()`, `initTabs()`; CSS `core/web/frontend/style.css` `.drawer-menu`, `.drawer-menu.open`, `.drawer-overlay`, transition declarations; wrappers `gui_entry.main()`, `gui_standalone.main()` load same frontend. | Existing implementation largely satisfies this; verify it is included in EXE bundle. If product expects persistent left rail rather than overlay drawer, adjust `.drawer-menu` / `main` layout. Ensure menu entries are not duplicated outside the drawer and tab switching closes drawer smoothly. | Run web/static tests if added; existing anchor: `python -m pytest tests/test_web_self_evolution.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual GUI: click `≡`, confirm left drawer slides open/closed smoothly and Dashboard/Chat/etc. live inside it. | Pending triage |
| CUR-DBG-016 | 35 | Better | Adjust the TUI `M` menu so first-level entries do not include workspace settings or LLM settings; centralize them under view switching, while shortcut-only items such as permissions are included in the menu. | `core.tui.menu_screens.MainMenuScreen.compose()`, `MainMenuScreen.on_list_view_selected()`, `ViewSelectMenuScreen.compose()`; `core.tui.app.SuperMedicineTUI.NAV_ITEMS`, `BINDINGS`, `action_open_menu()`; labels `core.tui.i18n.LABELS['menu_select_view']`, `menu_workspace_settings`, `menu_llm_settings`, `menu_permission_settings`; CSS `#tui-menu-panel`, `.tui-menu-list` in `core/tui/app.tcss`. | Remove `workspace-settings` from first-level `MainMenuScreen.compose()` and do not add LLM/workspace first-level entries. Keep workspace/LLM under `ViewSelectMenuScreen` via `NAV_ITEMS`. Decide whether permission appears as shortcut-only first-level action or only in view submenu, avoiding duplication contrary to policy. Update labels/tests/docs accordingly. | Run: `python -m pytest tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: press `M`; first level shows select view/theme/maximize/help/shortcut-only actions, while workspace/LLM are under select view. | Pending triage |
| CUR-DBG-017 | 40 | Rebuild | Rebuild the TUI 1:1 according to `https://github.com/anomalyco/opentui`, inheriting its interface and style; ask questions when unclear. | Full TUI surface: `core.tui.app.SuperMedicineTUI.compose()`, `BINDINGS`, `NAV_ITEMS`, status helpers; `core/tui/app.tcss`; widgets `core.tui.nav_widgets.NavItem`, `MenuButton`, `MenuOption`; menus `core.tui.menu_screens.*`; screens `core/tui/screens/*.py`; text `core/tui/i18n.py`; preview artifact path if recreated; docs/readme TUI sections. | This is a redesign program, not a blind patch. First confirm exact OpenTUI version/screenshots, whether Textual remains the framework, which OpenTUI components must be cloned, and legal/source boundary. Candidate repairs: replace app shell/sidebar/status/input layout in `SuperMedicineTUI.compose()` and `app.tcss`; rewrite nav/menu widgets; migrate each screen to OpenTUI-like style while preserving CLI/workspace/tool/log behavior. | Verification after clarified implementation: run `python -m pytest tests/test_tui.py tests/test_tui.py tests/test_tui.py tests/test_tui.py tests/test_tui.py tests/test_tui.py -q --override-ini addopts= -p no:cacheprovider --basetemp <tmp>`. Manual: visual comparison against approved OpenTUI reference screenshots/demo across all TUI screens. | Pending triage |

### Step 1 Current Context Summary

- Technology stack: Python package `supermedicine` Beta0.4.2 / `0.4.2b0`, Python `>=3.10`, setuptools build backend, Textual/Rich/PyYAML core dependencies, optional FastAPI/Uvicorn web GUI, optional R/rpy2 and figure dependencies, PyInstaller/pywebview EXE packaging path.
- Startup/context commands found in repository docs: `pip install -e .`, `python Install.py`, `supermedicine status`, `python Cli.py status`, `supermedicine diagnose`, `supermedicine tui`, `supermedicine tui --dry-run`, `python scripts/ci/build_gui_exe.py`.
- Test/quality commands documented for Tester ownership: `pip install -e ".[dev]"`, `ruff check --select=E,F,W --ignore=E501 .`, `python -m pip wheel . --no-deps --wheel-dir .pytest-tmp/wheel-smoke`, `pytest tests/ -v`; legacy `supermedicine test` exists but is not run in this Coder pass.
- Repository files absent at root during Step 1 context check: no `AGENTS.md`, no `package.json`, no `pom.xml`, no `build.gradle`, and no `*.sln`.

### Step 1 Current Gaps Summary

- All 17 current Debug.txt entries are pending triage only; no root-cause fix is implemented or claimed by this Step 1 archive.
- TUI items cluster around `core/tui/*`, `core/tui/screens/*`, and `core/tui/app.tcss`; GUI/EXE items cluster around `core/web/frontend/*`, `gui_entry.py`, `gui_standalone.py`, `assets/*`, `installer/*`, and `scripts/ci/build_gui_exe.py`; tool discovery clusters around `core/workspace_tools.py`, `cli/commands/tool.py`, and `plugins/tools/*`.

| ID | Type | Original source / evidence summary | Impact scope | Dependencies | Directly executable? | Needs user confirmation? | Suggested acceptance standard |
| --- | --- | --- | --- | --- | --- | --- | --- |
| DBG-BUG-001 | BUG | `Debug.txt` lines 3-4: permission switching is wrong; entering `FULL` does not switch to full access, may also send `FULL` into chat, conservative-mode accessible directory import fails, absolute/relative paths and quoted/unquoted paths should be recognized, and the TUI lower-left `P` permission display is visually misplaced. | Permission mode state, TUI permission screen/status display, chat input routing, path parsing for authorized roots, CLI/TUI permission parity. | `permission/access_mode.py`, `permission/engine.py`, `core/tui/permissions.py`, `core/tui/screens/permission_screen.py`, `core/tui/app.py`, `core/tui/state.py`, tests including `tests/test_permission_modes.py`, consolidated TUI coverage in `tests/test_tui.py`, and `tests/test_path_safety.py`. | Evidence first for runtime failure reproduction; then directly executable after a failing scenario is captured. | No for the stated desired behavior; yes only for final visual placement if multiple layouts are possible. | Full-mode confirmation entered in the permission UI changes the stored/accessed mode without leaking `FULL` into chat; conservative authorized-root import accepts absolute/relative paths with or without quotes; permission indicator is integrated into the selected minimal terminal layout; regression coverage proves chat input and permission confirmation are separated. |
| DBG-BUG-002 | BUG | `Debug.txt` lines 6-6: logs should be merged into one log record per application opening/session, and all logs before exit should be recorded there. | Log storage/session semantics, TUI stream logging, log report screen, CLI log commands. | `core/log_report.py`, `core/tui/app.py`, `core/tui/screens/log_screen.py`, `tests/test_log_report.py`, `tests/test_tui.py`, README log documentation. | Evidence first, because current log report semantics must be compared with the requested session-level aggregation. | Possibly, if "one log record" means one JSONL file per launch versus one JSON object/report per launch. | A single launch/session has one stable log container identifier; all redacted events from startup through normal exit append to that container; log screen and CLI can display/follow that session log; no sensitive data is persisted. |
| DBG-BUG-003 | BUG | `Debug.txt` lines 8-8: R/Python tools appear not to be recognized/imported; examples include heatmap and UMAP tools not being scanned. | Workspace tool discovery/import, plugin/tool manifests, Python/R tool templates, TUI tool screen. | `core/workspace_tools.py`, `core/tui/screens/tool_screen.py`, `plugins/tools/*/tool.yaml`, `plugins/tools/*/plugin.yaml`, `tests/test_workspace_full.py`, `tests/test_tui_*tool*` coverage where applicable. | Evidence first, because missing tools may be absent manifests, naming mismatch, scan path mismatch, or UI display issue. | No, unless deciding whether heatmap/UMAP should be built-in bundled tools or user-imported templates. | Tool scan reports all valid Python/R candidates under configured plugin/tool roots; invalid or missing heatmap/UMAP manifests produce actionable diagnostics; import works from scan selection without manually knowing IDs; tests or documented manual checks cover both Python and R tools. |
| DBG-BUG-004 | BUG | `Debug.txt` lines 10-10: LLM thinking process is invisible in the dialog; `Thinking` should be visible to the user with dynamic real-time updates. | LLM streaming/thinking events, chat display, TUI task-running UX, provider response handling. | `core/llm_client.py`, `core/llm_providers/*`, `core/kernel.py`, `core/tui/screens/chat_view.py`, `core/tui/screens/dialog_screen.py`, `core/tui/app.py`, `tests/test_tui.py`, `tests/test_llm_client.py`. | Evidence first, because current provider clients may not expose reasoning tokens/streaming and providers vary in whether thinking traces are available. | Yes for policy: whether to display raw model reasoning, summarized thinking, or progress-only status given provider/privacy constraints. | During long LLM calls, the dialog shows a live, clearly labeled thinking/progress area; the display degrades gracefully when a provider exposes no reasoning stream; no hidden chain-of-thought or sensitive internal prompt content is shown unless explicitly allowed by the final policy. |
| DBG-BUG-005 | BUG | `Debug.txt` lines 12-12: all dynamically changing data or files must be refreshed in real time. | TUI refresh hooks, file/data watchers, workspace/tool/log/dialog/dashboard reload paths, status updates. | `core/tui/app.py`, TUI screen modules, workspace/tool/log/dialog data loaders, tests covering dynamic refresh hooks and screen reload behavior. | Evidence first, because each dynamic surface needs inventory before changing refresh cadence. | Yes if real-time means polling interval, filesystem watching, or manual refresh plus activation refresh. | Dynamic TUI data/file surfaces refresh according to the confirmed real-time policy without stale displays, excessive polling, or lost user input; impacted screens have regression coverage or documented manual checks. |
| DBG-BET-001 | Better | `Debug.txt` lines 15-15: settings are currently confusing; all settings should be unified under `M`, invokable via a TUI upper-left button; menu localization is incomplete. | TUI global menu/navigation, settings screens, i18n strings, help text. | `core/tui/app.py`, `core/tui/i18n.py`, TUI screens, README TUI docs, `tests/test_tui_entrypoint.py`, `tests/test_tui_state.py`. | Directly executable for inventory and menu unification draft; implementation needs design preview if layout changes are large. | Yes for final menu structure and labels if UI changes are substantial. | Pressing `M`/`m` and the upper-left menu affordance open one settings/menu hub; localized labels are complete and consistent; settings are discoverable without conflicting shortcuts; README/Chinese docs describe the same flow. |
| DBG-BET-002 | Better | `Debug.txt` lines 17-17: shortcuts are confusing; shortcuts should only work in uppercase-letter state and should be callable when focus is outside the dialog input, with input-method compatibility. | TUI keybinding policy, focus handling, chat input, IME safety. | `core/tui/app.py`, `core/tui/screens/chat_view.py`, `core/tui/state.py`, `tests/test_tui.py`, `tests/test_tui_entrypoint.py`, `tests/test_tui_invalid_table_actions.py`. | Evidence first, because Textual key handling and IME behavior need precise reproduction and compatibility mapping. | Yes for exact uppercase-only shortcut policy and any exceptions such as `Esc`, `Tab`, arrows, or `?`. | Global alphabetic shortcuts trigger only outside the dialog input and only in the confirmed uppercase/modified state; Chinese/IME input remains ordinary text; shortcut help lists the final policy; automated or documented manual checks cover focus and input-method cases. |
| DBG-BET-003 | Better | `Debug.txt` lines 19-19: repository functions and structure are confused; deeply analyze the repository so every function is correctly positioned and optimized. | Whole repository architecture, module ownership, function map, docs-to-code alignment. | `FUNCTION_MAP.md`, `ARCHITECTURE.md`, `Architecture/MaintainerRepositoryReading.md`, `Architecture/RepositoryOptimizationAudit.md`, all source directories. | Evidence first; this is an audit/refactor program, not a single direct implementation item. | Yes before any restructuring that changes public paths or behavior. | A repository architecture audit maps each module to a clear responsibility, identifies misplaced/duplicated functions with evidence, and converts changes into small behavior-preserving tasks; no rename, move, or deletion occurs without reference synchronization and evidence. |
| DBG-BET-004 | Better | `Debug.txt` lines 21-21: beautify the TUI; first output a preview image to `C:\Users\D2O\Downloads` for correction/confirmation, then implement fully. User-confirmed style is Opencode-like minimal terminal. | TUI visual design, screenshots/previews, Textual CSS/layout, status/menu surfaces. | `core/tui/app.py`, `core/tui/app.tcss`, TUI screen modules, design preview artifact outside source tree, later user approval. | Directly executable for preview/mockup generation only; implementation blocked until user confirms preview. | Yes, explicitly required before implementation. | A preview/mockup is produced in `C:\Users\D2O\Downloads` and accepted by the user; implementation matches the approved minimal terminal style; TUI remains readable across core screens and permission/status cues remain visible. |
| DBG-BET-005 | Better | `Debug.txt` lines 23-23: design a project Logo that fits the theme and embed it into Markdown. User-confirmed direction is a minimal text logo. | Branding, README/docs visuals, release documentation. | `README.md`, future `README.zh-CN.md`, possibly `ARCHITECTURE.md`/release docs; no source behavior dependencies. | Directly executable for logo concepts; embedding requires selected final text logo. | Yes for final logo text/wordmark. | A minimal text logo/wordmark is approved; it appears consistently in English and Chinese Markdown entry docs without heavy binary assets; Markdown remains professional and release-ready. |
| DBG-BET-006 | Better | `Debug.txt` lines 25-25: Markdown formatting is confused; README and other Markdown files must reach professional release quality; language-specific docs must be separate, and mixed language is forbidden. Current evidence: `README.md` contains both English and Chinese sections; `README.zh-CN.md` is absent. | Release documentation, docs language policy, install/TUI/permission documentation, link consistency. | `README.md`, missing `README.zh-CN.md`, `INSTALL.md`, `ARCHITECTURE.md`, `FUNCTION_MAP.md`, other Markdown docs and manifests. | Directly executable for documentation split plan and draft; full rewrite should be staged to avoid losing content. | Yes for final wording/style and any logo placement. | `README.md` is English-only and release-grade; `README.zh-CN.md` exists and is Chinese-only; cross-links are correct; all command examples and policy statements stay consistent with source behavior; no source code behavior changes are made. |
| DBG-BET-007 | Better | `Debug.txt` lines 27-27: TUI-displayed titles and similar emphasis text must use single words with initial capitalization to distinguish key points. | TUI copy style, headings, labels, i18n strings, docs screenshots/previews. | `core/tui/i18n.py`, `core/tui/app.py`, TUI screen modules, README/TUI docs. | Evidence first for string inventory; directly executable after style rules are confirmed. | Yes for final copy style if localization conflicts with single-word English title casing. | TUI titles/key labels follow the confirmed single-word initial-capital style consistently without breaking Chinese/localized copy or accessibility. |
| DBG-BET-008 | Better | `Debug.txt` lines 29-29: add a Chat status checker that shows dynamic `Processing` while a dialog is running and temporarily locks the dialog box to prevent duplicate user input. | Chat view runtime state, LLM task lifecycle, input enable/disable behavior, status display. | `core/tui/screens/chat_view.py`, `core/tui/app.py`, `core/kernel.py`, LLM client/provider task surfaces, TUI chat tests. | Evidence first, because current in-flight task state and input handling must be mapped before locking behavior changes. | Yes if lock scope, cancellation behavior, or exact Processing display needs product approval. | While a chat request is in flight, the UI shows dynamic Processing status and prevents duplicate submission without losing typed text; input is restored on success, failure, or cancellation. |
| DBG-BET-009 | Better | `Debug.txt` lines 31-31: reduce unnecessary shortcuts; keep only important shortcuts such as `P` and `M`, and move reduced actions into the `M` menu. | TUI keybinding inventory, menu hub, help text, i18n, keyboard accessibility. | `core/tui/app.py`, TUI screens, `core/tui/i18n.py`, README/TUI docs, shortcut tests. | Evidence first for shortcut inventory; implementation depends on DBG-BET-001 menu structure. | Yes for final retained shortcut list and menu placement. | Only confirmed essential shortcuts remain globally active; removed shortcuts are reachable through the `M` menu and documented consistently. |
| DBG-BET-010 | Better | `Debug.txt` lines 33-33: write content into the project Wiki at `https://github.com/KarasawaYikiho/SuperMedicine/wiki` with professional, clear organization. User has confirmed GitHub Wiki publishing authorization. | GitHub Wiki documentation, release docs, project knowledge base. | GitHub Wiki access/workflow, README/architecture docs as source material. | Directly executable for preparing and publishing through the authorized GitHub Wiki workflow. | Yes for final wiki structure and wording if materially different from existing release docs; no longer blocked by publishing authorization. | Wiki pages are professionally organized, clear, consistent with release docs, and published through an approved GitHub Wiki workflow. |
| DBG-Q-001 | Question | `Debug.txt` lines 35-36: unclear whether multi-Agent is working normally or whether the system is always running a single-Agent dialog; investigate. | Agent orchestration, adapter subagent dispatch, TUI/CLI task execution paths, OpenCode optional integration. | `agents/orchestrator.py`, `agents/base_agent.py`, `agents/state_machine.py`, `agents/checkpoint.py`, `adapters/opencode/adapter.py`, `core/kernel.py`, tests `test_orchestrator.py`, `test_opencode_adapter.py`, `test_integration.py`. | Evidence first only; do not change behavior before proving the active path. | Possibly, if the product decision is to make multi-Agent the default rather than an optional/orchestrated mode. | A diagnostic report shows which user flows invoke `Orchestrator.dispatch`, which flows call `Kernel.execute_task`/LLM directly, and whether OpenCode adapter subagent dispatch is only metadata/degraded or active; any gap is converted into an implementation task with tests. |
| DBG-REB-001 | Rebuild | `Debug.txt` lines 38-39: review `https://github.com/anomalyco/opencode` deeply; align this project completely with Opencode. User-confirmed scope includes directory structure, TUI, permissions, Agent, and plugin mechanism. | External reference audit, architecture comparison, TUI/permissions/agents/plugins/adapters, docs and roadmap. | External Opencode repository review, `adapters/opencode/*`, `core/tui/*`, `permission/*`, `agents/*`, `plugins/*`, architecture docs. | Evidence first; must not copy external source or overclaim native runtime support. | Yes for each alignment decision that changes SuperMedicine identity, public API, or release scope. | A legally safe comparison matrix maps Opencode concepts to SuperMedicine equivalents/gaps; alignment tasks are prioritized by structure, TUI, permission, Agent, and plugin mechanism; optional adapter claims remain accurate and source-code copying is avoided. |
| DBG-REB-002 | Rebuild | `Debug.txt` lines 40-41: deeply analyze the whole repository and completely refactor function logic while preserving all functionality, reducing function count, improving efficiency, and making maintenance easier. User-confirmed boundary: only merge duplicated logic; dead code deletion requires evidence. | Whole repository callable graph, duplicate logic, performance hotspots, maintainability, tests. | `FUNCTION_MAP.md` callable inventory, architecture audit docs, all source/tests, repository optimization boundary docs. | Evidence first; no implementation until duplicate/dead-code evidence and acceptance tests are identified. | Yes for risky merges, deletions, or any behavior-visible changes. | A refactor baseline identifies exact duplicate logic and proven dead code with evidence, dependency impact, and coverage; each approved refactor preserves public behavior and passes targeted and broad regression gates owned by Tester; no deletion occurs solely because code “looks unused.” |

## Repository Baseline and Risk Map

| Baseline area | Current repository evidence | Risk noted for Debug.txt mapping | Follow-up ledger mapping |
| --- | --- | --- | --- |
| Entry points | `README.md` documents `supermedicine`, `python Cli.py`, `python install.py`, and `supermedicine tui`; `FUNCTION_MAP.md` identifies `Cli.py`, `Install.py`, `install.py`, and installer helpers as top-level surfaces. | CLI/TUI state parity matters for permissions, logs, tools, and docs. | DBG-BUG-001, DBG-BUG-002, DBG-BET-006 |
| TUI | `core/tui/app.py`, `app.tcss`, `i18n.py`, `state.py`, `permissions.py`, and screen modules are present; README describes Chinese Textual workbench and keybindings. | Requested Opencode-like minimal style, settings/menu consolidation, uppercase shortcut policy, thinking display, real-time refresh, title style, Processing lock, shortcut reduction, and permission display all touch shared TUI surfaces. | DBG-BUG-001, DBG-BUG-004, DBG-BUG-005, DBG-BET-001, DBG-BET-002, DBG-BET-004, DBG-BET-007, DBG-BET-008, DBG-BET-009 |
| Permissions | `permission/access_mode.py`, `policy.py`, `engine.py`, `audit.py`, `prompt_generator.py`, and `core/path_safety.py` define access modes, policy checks, audit, and path safety. | Full-mode confirmation, authorized root parsing, and TUI display must remain consistent with audit/path-safety semantics. | DBG-BUG-001, DBG-REB-001 |
| LLM | `core/llm_client.py`, `core/llm_manager.py`, and `core/llm_providers/*` manage providers and responses; README emphasizes provider configuration and redaction. | Thinking/progress visibility may require provider streaming/reasoning support and a policy for what is safe to expose. | DBG-BUG-004 |
| Logs | `core/log_report.py` and `core/tui/screens/log_screen.py` are documented as redacted log report and follow surfaces. | User wants one launch/session-level log container; current semantics need investigation before changing storage format. | DBG-BUG-002 |
| Plugins/tools | `core/plugin_registry.py`, `plugins/base_plugin.py`, plugin manifests, `plugins/tools/*`, and workspace tool helpers exist. | R/Python scan/import issue may be manifest-, path-, language-, or UI-selection-related. | DBG-BUG-003, DBG-REB-001 |
| Multi-Agent | `agents/*` implements abstract agents, checkpoints, orchestrator, and state machine; `adapters/opencode/*` documents optional OpenCode adapter/agents. | Need to prove whether normal chat uses orchestration or a single Kernel/LLM path. | DBG-Q-001, DBG-REB-001 |
| README/docs | `README.md` is present and mixed-language; `README.zh-CN.md` is absent; `FUNCTION_MAP.md` is authoritative function inventory. | User requires professional release-grade split-language Markdown, logo embedding, and professional Wiki content. | DBG-BET-005, DBG-BET-006, DBG-BET-010 |
| Tests/coverage | `tests/` contains permission, TUI, LLM, log, plugin/tool, orchestrator, repo hygiene, release, and regression baseline tests. | Future implementation tasks have adjacent coverage anchors, but this document does not run or modify tests. | All implementation follow-ups; especially DBG-BUG-001 through DBG-BUG-004 and DBG-Q-001 |

## CI Log Evidence from `LOG.zip`

| Evidence area | Observed CI evidence |
| --- | --- |
| Extracted log source | `C:\Users\D2O\Downloads\LOG.zip`, extracted for review under `C:\Users\D2O\AppData\Local\Temp\opencode\SuperMedicine_LOG_extract`. |
| Failing jobs / stages | `Release gate (windows-latest, Python 3.10)`, `Release gate (windows-latest, Python 3.11)`, and `Release gate (windows-latest, Python 3.12)` fail in the `Test suite` stage. |
| Passing contrast | `Release gate (windows-latest, Python 3.13)` shows the same workspace refresh test passing; sampled macOS/Linux logs did not show this failure. |
| Failing command | PowerShell test-suite command: `python -m pytest tests/ -v --tb=short --override-ini addopts= -p no:cacheprovider --basetemp $pytestTemp`. |
| Key assertion/error | `tests/test_tui.py::test_workspace_view_refresh_button_reads_external_workspace_created_after_enter` fails with `AssertionError: assert 0 == 1`; `DataTable(id='workspace-table').row_count` remains `0` after external workspace creation and clicking `#workspace-refresh`. The failed jobs end with `Process completed with exit code 1`. |
| Involved repository files identified by logs/evidence | Primary involved files: `tests/test_tui.py`, `core/tui/screens/workspace_screen.py`. Captured logging error paths also include `core/tui/screens/dashboard.py`, `core/llm_manager.py`, and `core/tui/screens/llm_screen.py`. |
| Additional captured error context | During async TUI/logging teardown, logs include `ValueError: I/O operation on closed file` from logging output while dashboard/LLM status paths are active. This is recorded as evidence, not as a proven root cause. |
| Root-cause candidates, evidence only | Candidate A: Windows/Textual async refresh timing or mounted-screen refresh ordering around workspace-table reload. Candidate B: logging handler lifecycle issue during async TUI test teardown. These are candidates only; no speculative source fix is authorized by this evidence section. |

## Grouping and Traceability Notes

- The ledger contains 18 entries, matching the 18 actionable/question/rebuild items in `Debug.txt` lines 4, 6, 8, 10, 12, 15, 17, 19, 21, 23, 25, 27, 29, 31, 33, 36, 39, and 41.
- No `Debug.txt` item is split into multiple ledger IDs; compound details are preserved inside the corresponding evidence and acceptance fields.
- Related work is mapped in the baseline table but not merged, so Tester can trace each original item exactly once.
- This document intentionally records future acceptance standards and dependencies only; it does not authorize immediate source modification, deletion, external-source copying, or behavior change.

## Step 2 Implementation Evidence Pass

**Review date:** 2026-06-08
**Purpose:** 对照 `C:\Users\D2O\Desktop\Debug.txt` 18 项台账逐条核验当前仓库落实状态，补齐最小必要缺口，并给 Tester 提供源码/测试/配置/文档定位。未处理 CI 根因；仅记录 Debug.txt 明确项的落实情况。

| ID | Step 2 status | Evidence / location | Step 2 action |
| --- | --- | --- | --- |
| DBG-BUG-001 | 已修复/已落实 | `core/tui/screens/permission_screen.py` uses a dedicated `#permission-confirm-input` and requires `FULL` before `PermissionScreenController.set_mode("full")`; `core/tui/app.py` routes only `#prompt-input` submissions into chat, so permission confirmation is not chat text; `core/config_center.py` persists shared file-access mode and authorized roots; `tests/test_tui.py` covers FULL confirmation and policy update; `README.md`/`README.zh-CN.md` document permission entry and FULL confirmation. | Minimal补齐：`core/config_center.py` now normalizes quoted/unquoted user-entered directory paths before authorize/revoke, so absolute/relative paths with optional quotes reach the same resolved directory path. TUI permission status remains integrated in the status bar through `_permission_status_label()`, plus the sidebar permission entry. |
| DBG-BUG-002 | 已落实 | `core/log_report.py` writes `session-<session_id>.json` and appends session records under one container; `TUI_LOG_SESSION_ID = "tui-application"`; `configure_tui_log_storage(...)` is called in `core/tui/app.py` before interactive launch; `tests/test_log_report.py::test_session_writes_append_to_one_redacted_log` covers same-session append; `README.md`/`README.zh-CN.md` document `.supermedicine/logs/` and `log follow --session-id`. | 无需修改；current implementation already satisfies one stable session container for session-scoped logs and TUI launch log storage. |
| DBG-BUG-003 | 已落实 | `core/workspace_tools.py` supports Python/R languages, scans `plugins/tools/*`, accepts `tool.yaml` or `plugin.yaml`, exposes `BUILTIN_TEMPLATES` for Python/R `heatmap` and `umap`, and imports scanned candidates by index/slug; bundled manifests exist at `plugins/tools/python_data_analysis/tool.yaml` and `plugins/tools/r_data_analysis/tool.yaml`; `tests/test_workspace_full.py` covers Python/R built-in heatmap/UMAP scaffolds and scan/import candidates; README tool section documents scan/add selection. | 无需修改；heatmap/UMAP and Python/R scanning are implemented as templates and candidate scan/import flow. |
| DBG-BUG-004 | 已落实（安全进度/Thinking surface） | `core/tui/screens/chat_view.py` exposes `add_reasoning_status(...)`; `core/tui/app.py::_run_kernel_task()` renders `reasoning`, `status`, `assistant_start`, and `assistant_delta` events through `progress_callback`; `core/kernel.py::execute_task()` accepts `progress_callback`; `tests/test_tui.py` covers reasoning/progress display and streaming methods. | 无需修改；implementation uses provider-safe visible progress rather than hidden chain-of-thought exposure. |
| DBG-BUG-005 | 已落实（targeted refresh policy） | `core/tui/app.py::_refresh_visible_dynamic_data()` invokes view refresh hooks on mount/switch; workspace/log/tool/dialog/dashboard screens expose refresh hooks/buttons; `tests/test_tui.py` and `tests/test_tui.py` cover manual/activation refresh; README states targeted refresh without watchers/polling. | 无需修改；CI root-cause work is intentionally out of scope except as already documented evidence. |
| DBG-BET-001 | 已落实（menu hub） | `core/tui/app.py` has `MainMenuScreen`, `ViewSelectMenuScreen`, `action_open_menu()`, and menu entries for view selection/theme/help/maximize; `core/tui/i18n.py` localizes menu labels; README TUI section documents `M` menu. | Minimal补齐同 DBG-BET-002/009：shortcut labels/docs now use uppercase `M` policy. |
| DBG-BET-002 | 已修复/已落实 | `core/tui/app.py` PromptInput protects ordinary text, backspace, terminal controls, and IME-like input; numeric direct switching was removed; README documents input preservation. | Minimal补齐：global alphabetic bindings are now declared as uppercase `M`/`P`/`Q`, and TUI labels/docs explicitly describe uppercase-only shortcut policy. |
| DBG-BET-003 | 已落实为审计/无需重构 | `FUNCTION_MAP.md`, `ARCHITECTURE.md`, `Architecture/MaintainerRepositoryReading.md`, and `Architecture/RepositoryOptimizationAudit.md` map repository responsibilities and preserve no-go boundaries. | 无需源代码重构；Debug.txt asked for deep analysis, and current evidence shows the repository already has audit artifacts. Broad restructuring remains blocked without approved scoped tasks. |
| DBG-BET-004 | 部分落实/需用户确认 | TUI minimal terminal direction is implemented in `core/tui/app.py` theme and `core/tui/app.tcss`; user-required preview artifact in `C:\Users\D2O\Downloads` is not a repository file and final visual implementation remains confirmation-gated. | 无代码修改；status recorded as confirmation-gated, not silently completed. |
| DBG-BET-005 | 已落实 | Minimal text logo/wordmark is embedded at the top of `README.md` and `README.zh-CN.md` as centered Markdown/HTML text, avoiding heavy binary assets. | 无需修改。 |
| DBG-BET-006 | 已落实 | `README.md` is English-only, `README.zh-CN.md` is Chinese-only, both include release-grade sections for positioning, install, quick start, permissions, tools, TUI, LLM, logs, adapters, quality gate, release info, troubleshooting, and license. | 无需修改。 |
| DBG-BET-007 | 部分落实/样式受本地化约束 | Key chat labels use single capitalized English words in `core/tui/i18n.py` (`User`, `System`, `Assistant`, `Error`, `Status`, `Output`); Chinese TUI navigation remains localized for usability. | 无源代码修改；single-word capitalization is satisfied where English emphasis labels are used, while Chinese localization is preserved. |
| DBG-BET-008 | 已落实 | `core/tui/app.py` tracks `_chat_processing`, uses `_begin_chat_processing()`, disables only `#prompt-input`, displays `Chat Processing` in status text, rejects duplicate submissions, and unlocks in `finally`; `tests/test_tui.py` covers processing lock/reject/unlock/status; README documents the behavior. | 无需修改。 |
| DBG-BET-009 | 已修复/已落实 | `core/tui/app.py` global bindings are limited to important actions plus quit; menu contains secondary actions; README states numeric keys are not direct shortcuts and secondary actions live under menu. | Minimal补齐：bindings and docs now use uppercase `M`/`P`/`Q`, matching the Debug.txt shortcut policy while preserving menu access. |
| DBG-BET-010 | 已发布证据已记录 | Professional source docs exist in `README.md`, `README.zh-CN.md`, `ARCHITECTURE.md`, `INSTALL.md`, and architecture audits and can seed Wiki pages; user has confirmed GitHub Wiki publishing authorization and Step 4 records remote publication evidence at commit `d6a1e11`. | Repository tracking now records the remote Wiki publication evidence. Local source/tests still cannot prove remote content availability after future edits; verify through GitHub Wiki remote/history when needed. |
| DBG-Q-001 | 已核查/无需修改 | `agents/orchestrator.py` implements in-process orchestration; `core/kernel.py::execute_task()` is the normal CLI/TUI execution path; `adapters/opencode/adapter.py` can dispatch via injected orchestrator but otherwise uses metadata fallback; README and `Architecture/PlatformIntegrationAudit.md` state native platform subagent runtimes are not launched by adapters alone. | 无需修改；current docs accurately distinguish available orchestration components from normal single-kernel chat path. |
| DBG-REB-001 | 已落实为安全对齐审计/无需外部源码复制 | `ARCHITECTURE.md` and `Architecture/PlatformIntegrationAudit.md` document optional OpenCode add-on boundaries, no native runtime overclaim, adapter capability limits, permissions, and legal/source-code boundary; README describes OpenCode integrations as optional adapter surfaces. | 无需修改；do not fetch/copy external OpenCode source in this step. |
| DBG-REB-002 | 已落实为审计/无需重构 | `Architecture/RepositoryOptimizationAudit.md` records duplicate/refactor policy, no-safe-rename decisions, generated-artifact cleanup policy, and behavior-preserving boundaries; `FUNCTION_MAP.md` provides callable inventory. | 无需源码重构；Debug.txt's broad refactor remains evidence/approval-gated and cannot be bundled into this minimal pass. |

### Step 2 Notes

- Items not fully source-complete are explicitly marked `部分落实/需用户确认`, external/confirmation-gated, or publication-evidence-tracked with a reason; no item is left without a status or evidence path, and the Wiki item is no longer blocked by authorization.
- The only source behavior changes in this pass are minimal Debug.txt gap closures: quoted directory-path normalization for permission roots and uppercase-only global shortcut binding/documentation. No unrelated refactor or CI root-cause repair was attempted.

## Step 2 Deep Source Code Audit

**Review date:** 2026-06-08
**Purpose:** 对 18 个 `Debug.txt` 条目逐项核对实际源码实现，而非仅引用文档。每项均记录源码路径、函数/类/模块、核心逻辑映射；未由代码实现、仅有文档/外部流程、部分实现或需用户确认的内容均标记为 code gap，供后续修复/确认任务使用。

| ID | Code-level implementation status | Concrete source evidence and logic mapping | Code gap for later repair / confirmation |
| --- | --- | --- | --- |
| DBG-BUG-001 | Implemented for functional permission switching/path normalization; UI placement is baseline-only. | `core/tui/screens/permission_screen.py::PermissionScreenController.set_mode()` normalizes the selected mode and requires `confirmation_text.strip() == "FULL"` before `ConfigCenter.set_file_access_mode(... explicit_confirmation=True)` can persist full mode. `PermissionView.compose()` declares a dedicated `Input(id="permission-confirm-input")`; `PermissionView._set_mode_from_form()` reads that input, clears it after success, refreshes permission state, and calls `_refresh_shell_status()`. `core/tui/app.py::SuperMedicineTUI.on_input_submitted()` returns unless `event.input.id == "prompt-input"`, preventing the permission `FULL` text from becoming chat content. `core/config_center.py::ConfigCenter.authorize_external_file_access_directory()` and `revoke_external_file_access_directory()` call `_normalize_user_directory_path()`, which uses `shlex.split(..., posix=False)`, strips surrounding quotes, expands `~`, resolves relative paths, and deduplicates stored root strings. `ConfigCenter.get_file_access_policy()` maps stored mode/roots into `AccessModePolicy`. `SuperMedicineTUI._permission_status_label()` reads `ConfigCenter.get_permission_mode_label()` into the status bar, and `SuperMedicineTUI.compose()` exposes the sidebar `P 权限` entry. | No code gap for the requested functional bugs. Remaining visual gap: no code or artifact proves a final user-approved lower-left/upper-left permission indicator placement beyond current status-bar/sidebar integration. |
| DBG-BUG-002 | Implemented as a stable TUI session log container, not a new process-unique physical file per launch. | `core/log_report.py` defines `TUI_LOG_SESSION_ID = "tui-application"`; `resolve_log_storage_locations()` maps it to `.supermedicine/logs/session-tui-application.json`. `LogReportStore.write(... session_id=...)` appends session-scoped records to the same safe JSON session file while non-session writes remain isolated. `core/tui/app.py::launch_tui()` calls `configure_tui_log_storage(root)` before interactive launch. `_TUILogTextSink` and `_capture_current_thread_tui_streams()` route worker-thread stdout/stderr into `append_tui_stream_output()`, so TUI kernel output is written through the same log-report storage surface. `core/tui/screens/log_screen.py::LogReportView.refresh_logs()` reads the store and refreshes/follows entries in the log screen. | Potential semantic gap only if the user means one unique log file per application opening rather than the current stable `session-tui-application.json` session container. |
| DBG-BUG-003 | Implemented for Python/R recognition, heatmap/UMAP templates, manifest scanning, and import selection. | `core/workspace_tools.py` sets `SUPPORTED_LANGUAGES` validation through `validate_language()`. `BUILTIN_TEMPLATES` contains four concrete templates: `("python", "heatmap")`, `("python", "umap")`, `("r", "heatmap")`, and `("r", "umap")`, each with `tool.yaml` and matching `runner.py`/`runner.R`. `WorkspaceToolService.tool_source_root()` scans `plugins/tools`; `scan_import_candidates()` iterates source directories; `_candidate_from_source()` prefers `tool.yaml`, falls back to `plugin.yaml`, infers R from `r_` prefixes, validates entrypoints and suffixes, and reports warnings/status. `import_scanned_tools()` resolves candidates by index/slug and copies importable source via `_import_candidate()`. TUI handoff exists in `core/tui/screens/tool_screen.py::_scan_candidates()` and `_add_selected_tool()`, which display scan results and import selected candidates. Bundled manifest evidence exists under `plugins/tools/python_data_analysis/tool.yaml`, `plugins/tools/r_data_analysis/tool.yaml`, `plugins/tools/r_template/tool.yaml`, `plugins/tools/python_stats/plugin.yaml`, and `plugins/tools/r_survival/plugin.yaml`. | No code gap for built-in heatmap/UMAP templates and Python/R scan/import. If a user-specific tool still fails, the later task must inspect that tool's concrete manifest/entrypoint and scan warning. |
| DBG-BUG-004 | Implemented as provider-safe visible progress/Thinking, not raw hidden chain-of-thought. | `core/kernel.py::Kernel.execute_task(... progress_callback=...)` defines `emit()` and sends status events during dispatch, permission checks, plugin execution, and LLM routing. `_execute_llm_chat()` emits a `reasoning` message before provider execution, emits `status` while waiting, emits `assistant_start` for streaming providers, and emits `assistant_delta` for stream chunks. `core/tui/app.py::_run_kernel_task()` passes a thread-safe `progress_callback` and `render_progress()` maps `reasoning` to `chat_view.add_reasoning_status()`, `status` to `add_status_message()`, `assistant_start` to `begin_assistant_message()`, and `assistant_delta` to `append_assistant_delta()`. `core/tui/screens/chat_view.py::add_reasoning_status()` and `append_assistant_delta()` provide the visible chat-surface methods. | No code gap for safe dynamic progress. Gap remains only if product policy later requires raw provider reasoning/chain-of-thought; current code intentionally displays a compliant progress fallback. |
| DBG-BUG-005 | Partially implemented under a targeted refresh policy; universal real-time watcher/polling is not implemented. | `core/tui/app.py::_refresh_visible_dynamic_data()` calls a mounted view's `refresh_view_data()` hook when switching/reopening views, then falls back to `_load_workspaces()` for legacy workspace views. `action_switch_view()` invokes this hook for same-view and new-view activation. `core/tui/screens/workspace_screen.py::WorkspaceView.refresh_view_data()` and button `#workspace-refresh` call `_load_workspaces(refreshed=True)`. `core/tui/screens/log_screen.py::LogReportView.refresh_view_data()` calls `refresh_logs(refreshed=True)` and supports auto-follow positioning. `core/tui/screens/dashboard.py::collect_dashboard_context()` recomputes workspace/plugin/module/LLM/token context for dashboard refresh. `core/tui/screens/tool_screen.py` exposes `#tool-refresh`/scan flows, and `core/tui/screens/dialog_screen.py` exposes `#dialog-refresh`. | Code gap: no repository code implements filesystem watchers or periodic polling for every dynamically changing data/file surface. Current implementation is activation/manual targeted refresh only; later repair needs a confirmed real-time policy and stale-surface inventory. |
| DBG-BET-001 | Implemented for keyboard menu hub, localized labels, and a literal clickable upper-left affordance. | `core/tui/app.py::MainMenuScreen` and `ViewSelectMenuScreen` implement modal menu navigation. `SuperMedicineTUI.BINDINGS` registers uppercase `Binding("M", "open_menu", ...)`; `action_open_menu()` opens the menu. `core/tui/app.py::MenuButton` handles mouse `on_click()` by calling `action_open_menu()`, and `SuperMedicineTUI.compose()` mounts `MenuButton("≡ 菜单 (M)", id="menu-button", classes="menu-affordance")` at the top of the sidebar before the title. `core/tui/app.tcss` styles `#menu-button` and `.menu-affordance`; `tests/test_tui_entrypoint.py::test_tui_upper_left_menu_button_opens_menu` covers the clickable entry. | No code gap for a minimal visible/clickable upper-left menu affordance. Visual polish still belongs to the preview/approval workflow if user requests a different placement or look. |
| DBG-BET-002 | Implemented for uppercase global alphabetic bindings and prompt-input preservation. | `core/tui/app.py::SuperMedicineTUI.BINDINGS` contains only uppercase alphabetic global bindings `Q`, `M`, and `P`. `PromptInput` filters terminal control sequences while preserving ordinary/lowercase/IME-like text; `on_input_submitted()` sends only `#prompt-input` content as chat. `core/tui/i18n.py::status_shortcuts_hint` and `help_global` state the uppercase-only policy. Numeric view switching is not present in `BINDINGS`; number labels are shown only as sidebar item text. | Code gap: no automated OS-level IME integration code/test is visible; policy is implemented at Textual keybinding/input-filter level. |
| DBG-BET-003 | Implemented only as audit artifacts and repository boundaries; no broad code restructure has been performed. | Codebase ownership anchors remain in `core/**`, `permission/**`, `agents/**`, `plugins/**`, and `adapters/**`. `FUNCTION_MAP.md` is the static callable inventory. `Architecture/RepositoryOptimizationAudit.md` records no-safe-rename decisions, duplicate/dead-code policy, generated-artifact cleanup policy, and behavior-preserving boundaries. `ARCHITECTURE.md` and `Architecture/MaintainerRepositoryReading.md` provide module responsibility context. | Code gap by design: no source code has been repositioned/optimized for every function. Any future refactor requires exact duplicate/dead-code evidence, acceptance coverage, and approval. |
| DBG-BET-004 | Partially implemented as current minimal TUI styling plus a lightweight preview-artifact workflow; user approval remains unclaimed. | `core/tui/app.py::SuperMedicineTUI.__init__()` registers the dark minimal `Theme` with muted colors and accent. `core/tui/app.tcss` implements a terminal-like vertical layout, compact header/footer, sidebar, status bar, menu panel, upper-left menu button, and input bar. `scripts/tui_preview_artifact.py` writes a text preview artifact to `Path.home() / "Downloads"` by default, recording dry-run shell/status/menu/refresh metadata without requiring image rendering; `tests/test_tui_entrypoint.py::test_tui_preview_artifact_workflow_writes_text_without_approval_claim` covers the workflow in a temp output dir. | Remaining gap: no actual user approval is recorded, and no image/screenshot artifact is generated if a visual image is specifically required. Full beautification remains confirmation-gated. |
| DBG-BET-005 | Implemented as Markdown minimal text logo/wordmark, not source-code behavior. | `README.md` and `README.zh-CN.md` both start with centered HTML/Markdown wordmark `<p align="center"><strong>SuperMedicine</strong><br><sub>minimal medical research workspace</sub></p>`. No binary logo asset is required by current implementation. | No source-code gap because this is documentation/branding. If a different visual logo is required, later work needs concept approval and doc embedding. |
| DBG-BET-006 | Implemented for main README language split; broad Markdown quality remains a documentation scope, not runtime code. | `README.md` is English release entry content; `README.zh-CN.md` is Chinese release entry content. Both carry install, TUI, permissions, tools, logs, adapters, quality/release, troubleshooting, and license content. Source-code behavior is not required for this item. | Code gap not applicable. Documentation gap remains if every Markdown file and remote Wiki must undergo a separate professional editorial pass. |
| DBG-BET-007 | Implemented for enforceable English emphasis labels with a code-backed Chinese-first inventory boundary. | `core/tui/i18n.py` defines `TUI_ENGLISH_TITLE_STYLE_KEYS`, `TUI_CHINESE_FIRST_TITLE_KEYS`, and `tui_title_style_inventory()`. Retained English emphasis labels (`User`, `System`, `Assistant`, `Error`, `Status`, `Output`) are checked as single capitalized words, while Chinese navigation/screen titles such as `状态看板`, `工作区`, and `对话历史` remain intentionally localized. `tests/test_tui_entrypoint.py::test_tui_title_style_inventory_enforces_english_emphasis_without_replacing_chinese` covers the inventory/enforcement boundary. | Remaining boundary: no full English-only style sweep is performed because Chinese-first localization is preserved; changing Chinese titles into single English words still requires product approval. |
| DBG-BET-008 | Implemented for dynamic Chat Processing status and duplicate-submit lock. | `core/tui/app.py` initializes `_chat_processing = False`; `_begin_chat_processing()` refuses a second request; `_set_chat_processing()` updates `_chat_processing`, `_task_running`, prompt disabled state, placeholder text, and status bar; `_update_prompt_input_lock()` disables only `#prompt-input`. `on_input_submitted()` rejects submissions while processing with `chat_processing_reject`. `_process_message()` wraps worker launch in the guard. `_run_kernel_task()` resets `_set_chat_processing(False)` in `finally`, covering success and exceptions. `status_text()` displays `t("chat_processing_state")`, and `core/tui/i18n.py` maps it to `Chat Processing`. | No code gap for basic processing status/lock. Future cancellation behavior would need separate design. |
| DBG-BET-009 | Implemented for reduced global shortcuts and menu relocation baseline. | `SuperMedicineTUI.BINDINGS` has only uppercase `Q`, `M`, and `P` as global alphabetic shortcuts. `MainMenuScreen` exposes secondary actions including view selection, theme change, maximize/restore, and help. `core/tui/i18n.py::help_global` tells users that help/theme/maximize are entered through `M` menu, while numbers/ordinary text remain input. | No current code gap for shortcut reduction baseline. Any future shortcut addition must update menu/docs/tests together. |
| DBG-BET-010 | Remote publication evidence recorded in repository tracking; no source module writes Wiki pages. | Repository source docs that can seed Wiki content exist in `README.md`, `README.zh-CN.md`, `ARCHITECTURE.md`, `INSTALL.md`, `FUNCTION_MAP.md`, and Architecture audit docs. User reports the remote GitHub Wiki is now published at commit `d6a1e11`; this ledger and `Architecture/DebugBugFixPlan.md` record that evidence for Tester/user traceability. | Remaining external boundary: repository tests cannot prove remote Wiki availability; future edits must inspect the GitHub Wiki remote or its `wiki.git` history instead of relying only on local docs. |
| DBG-Q-001 | Diagnostic implemented: orchestrator exists, normal chat path remains direct Kernel, adapter dispatch is orchestrator-backed only when injected. | `agents/orchestrator.py::Orchestrator.dispatch()` executes a registered `BaseAgent`, records state-machine/checkpoint stages, and returns task/agent/state metadata. Normal TUI chat enters `core/tui/app.py::_run_kernel_task()`, instantiates `core.kernel.Kernel`, and calls `Kernel.execute_task()` directly; `Cli.py` routes normal run behavior through Kernel as documented. `adapters/opencode/adapter.py::OpenCodeAdapter.subagent_dispatch()` calls `self._orchestrator.dispatch(agent_id, task)` only when `_orchestrator` is supplied; otherwise it returns `status: "degraded"`, `native_dispatch_executed: False`, and local role-context metadata. | No bug fix gap. Product gap: if multi-Agent should become default chat routing, a new mode/UX/API/tests task is required. |
| DBG-REB-001 | Partially implemented as optional OpenCode adapter/alignment boundary; full external Opencode structural alignment is not implemented. | OpenCode-related source anchors exist in `adapters/opencode/adapter.py`, `adapters/opencode/plugin.json`, `adapters/opencode/agents/*.md`, `adapters/opencode/skills/*.md`, plus shared `permission/**`, `agents/**`, `plugins/**`, and `core/tui/**`. `OpenCodeAdapter.capabilities()` truthfully reports optional add-on status, permission-checked dangerous tools, degraded dispatch without orchestrator, `native_opencode_subagent_runtime: False`, and one user-facing SuperMedicine agent. `Architecture/PlatformIntegrationAudit.md` records core/add-on boundaries and current gaps. | Code gap: no fresh full comparison against `https://github.com/anomalyco/opencode` is represented in source, and no complete directory/TUI/permission/Agent/plugin alignment has been implemented. External-source review/alignment remains legal-safe audit work. |
| DBG-REB-002 | Implemented only as evidence/guardrail documents; no whole-repo refactor executed. | `Architecture/RepositoryOptimizationAudit.md` records no source/content deletion, no safe rename, duplicate-reduction decisions, generated artifact cleanup policy, and public API/CLI/plugin/adapter/permission/test/config preservation boundaries. `FUNCTION_MAP.md` remains the callable inventory for source review. Runtime packages/functions under `core/`, `permission/`, `agents/`, `plugins/`, and `adapters/` remain in place. | Code gap by design: no function-count reduction, duplicate merge, dead-code deletion, or efficiency refactor is code-complete. Future refactor needs exact evidence, coverage, and approval. |

### Step 2 Code Gaps Summary

- `DBG-BUG-005`: universal real-time watcher/polling is missing; current code is activation/manual targeted refresh only, now with `SuperMedicineTUI.dynamic_refresh_surfaces()` inventory evidence.
- `DBG-BET-002`: uppercase shortcut policy exists, but there is no OS-level IME integration proof in source.
- `DBG-BET-003` and `DBG-REB-002`: broad repository repositioning/refactor is not implemented and remains approval/evidence-gated.
- `DBG-BET-004`: text preview artifact workflow exists through `scripts/tui_preview_artifact.py`, but screenshot/image rendering and user approval remain external/confirmation-gated.
- `DBG-BET-007`: English emphasis-label inventory/enforcement exists; a full English-only style sweep remains blocked by Chinese-first localization constraints.
- `DBG-BET-010`: Wiki publication evidence is now recorded as remote commit `d6a1e11`; local tests still cannot prove the remote Wiki content.
- `DBG-REB-001`: full legal-safe external Opencode comparison/alignment is not implemented; current code only provides optional adapter surfaces and boundaries.

## Step 4 Code Gap Closure Update

**Review date:** 2026-06-08
**Purpose:** 使用 Step 2/3 已确认缺口进行最小可本地实现补齐，同时保持外部/审批门控事项不被误报完成。

| ID | Step 4 local change | Remaining explicit boundary |
| --- | --- | --- |
| DBG-BET-010 | Project tracking now records that the GitHub Wiki has been published remotely at commit `d6a1e11`. | Remote Wiki content remains external; local repository tests cannot verify GitHub availability or future remote edits. |
| DBG-BET-004 | Added `scripts/tui_preview_artifact.py`, which can write a text preview artifact to `C:\Users\D2O\Downloads` by default and records dry-run TUI/menu/refresh metadata without image rendering. | The artifact explicitly says user approval has **not** been recorded; image rendering/screenshot capture remains optional/manual. |
| DBG-BET-001 | Added `MenuButton` and `#menu-button` (`≡ 菜单 (M)`) at the upper-left sidebar position; clicking it opens `MainMenuScreen`, while uppercase `M` remains the keyboard fallback. | Further visual redesign requires preview approval, not a hidden layout sweep. |
| DBG-BUG-005 | Added `DynamicRefreshSurface` inventory through `SuperMedicineTUI.dynamic_refresh_surfaces()` and covered it in tests, documenting workspace/log/dashboard/tool/dialog targeted refresh hooks and the no-polling/no-watcher boundary. | No broad filesystem watcher or periodic polling was added because prior constraints require targeted refresh only unless a stale surface and policy are confirmed. |
| DBG-BET-007 | Added `tui_title_style_inventory()` plus an enforcement test for retained English single-word labels while preserving Chinese-first navigation/screen titles. | A full conversion of Chinese titles to English single words remains product/locale gated. |

## Step 4 Regression Coverage Evidence Pass

**Review date:** 2026-06-08
**Purpose:** 对 Step 2 Debug.txt 修正与 Step 3 CI 根因修复进行回归覆盖核查，仅在缺口处最小补充测试，不做无关重构。

| Coverage target | Evidence / action |
| --- | --- |
| 权限路径带引号归一化 | 补充 `tests/test_config_center.py::test_file_access_authorized_roots_normalize_quoted_absolute_and_relative_paths`，覆盖带引号绝对路径授权、带引号相对路径去重与撤销，并验证 conservative policy 对归一化目录写入放行。 |
| uppercase-only `M`/`P`/`Q` 与 lowercase ordinary input | 已有 `tests/test_tui_entrypoint.py::test_tui_help_text_documents_actual_bindings_and_state_meanings` 断言绑定键为 uppercase `Q`/`M`/`P`；`test_prompt_input_treats_m_and_shift_m_as_menu_key` 覆盖 uppercase/Shift `M` 菜单；`test_prompt_input_treats_lowercase_m_as_ordinary_text` 与 `test_prompt_input_keeps_removed_shortcuts_and_ordinary_keys_as_text` 覆盖 lowercase `m`/`p` 和普通键不被当作终端控制或菜单快捷键。无需新增测试。 |
| workspace refresh CI race | 已有 `tests/test_tui.py::_wait_for_tui_condition` 和 `test_workspace_view_refresh_button_reads_external_workspace_created_after_enter` 使用 condition-based wait 等待 refresh 后 `DataTable.row_count == 1`，覆盖外部创建工作区后刷新按钮读取新数据的 Windows/Textual 异步竞态路径。无需新增测试。 |

## Step 3 Deep Test Coverage Audit

**Review date:** 2026-06-08
**Purpose:** 深度核对 18 个 `Debug.txt` 条目的真实测试覆盖，确认覆盖的是可执行行为而非仅文档文本。仅当发现可本地实现且有价值的行为缺口时才补测试；对 Wiki 远程发布、预览审批、OS 级 IME 证明、外部 Opencode 全量比较、全仓库重构等外部/人工/审批门控项目，记录为何自动化测试不能完整证明，以及替代验证方式。

### Step 3 Coverage Matrix

| ID | Executable behavior evidence | Step 3 audit result | Remaining manual/external rationale |
| --- | --- | --- | --- |
| DBG-BUG-001 | `tests/test_tui.py::test_permission_screen_controller_requires_full_confirmation_and_updates_policy` proves `FULL` requires exact confirmation and updates the effective policy. `tests/test_config_center.py::test_file_access_policy_switches_runtime_without_restart_and_requires_full_confirmation` proves full mode cannot be enabled without explicit confirmation and authorized roots affect policy decisions. `tests/test_config_center.py::test_file_access_authorized_roots_normalize_quoted_absolute_and_relative_paths` proves quoted absolute and quoted relative directories normalize, deduplicate, allow policy writes, and revoke correctly. | Coverage is behavior-level and sufficient for the functional bug: mode transition, confirmation gate, policy effect, and root normalization are exercised. No new test added. | Final visual placement of the permission indicator remains a screenshot/layout approval matter; automated tests can confirm widgets/status text exist, not user acceptance of a lower-left visual preference. |
| DBG-BUG-002 | `tests/test_log_report.py::test_session_writes_append_to_one_redacted_log` proves two writes with the same session id append into one `session-<id>.json` and remain redacted. `tests/test_tui.py::test_log_screen_populated_table_and_detail_statistics_match_selected_entry` proves the TUI log screen reads session records and reports entry/severity statistics. | Coverage is behavior-level for session container aggregation, redaction, display, and detail selection. No new test added. | If the product later redefines “one log per opening” as a unique physical file per launch instead of the current stable session container, that semantic change needs new acceptance tests. |
| DBG-BUG-003 | `tests/test_workspace_full.py::test_tool_init_creates_python_and_r_directories_under_workspace`, `test_builtin_templates_can_be_scaffolded_and_loaded`, `test_scan_import_candidates_lists_python_and_r_with_metadata_fallback`, `test_import_scanned_tools_by_selection_rejects_invalid_candidate`, and CLI tool-add tests prove Python/R language handling, heatmap/UMAP template scaffolding, `tool.yaml`/`plugin.yaml` scan fallback, import selection, invalid-candidate diagnostics, and workspace import side effects. | Coverage is behavior-level for the reported scan/import class; no superficial doc-only test added. No new test added. | User-specific unrecognized external tools still require the concrete manifest/entrypoint and scan output before writing a targeted regression. |
| DBG-BUG-004 | `tests/test_tui.py` covers kernel progress callback rendering (`reasoning` event), visible status updates, streaming assistant deltas, safe escaping/redaction, and task completion status. | Coverage is behavior-level for provider-safe Thinking/progress display and streaming UI methods. No new test added. | Raw provider chain-of-thought exposure is intentionally not automated because it is policy/provider dependent and should not be asserted unless explicitly approved as displayable content. |
| DBG-BUG-005 | `tests/test_tui.py::test_workspace_view_refresh_button_reads_external_workspace_created_after_enter` proves external workspace creation becomes visible after refresh with condition-based TUI waiting. `tests/test_tui.py::test_log_screen_refresh_button_reads_entries_created_after_enter` proves external log writes become visible after refresh. `tests/test_tui.py::test_dashboard_view_exposes_activation_refresh_hook` proves dashboard exposes activation refresh. | Coverage is behavior-level for the implemented targeted refresh policy. No new test added because universal watcher/polling is not implemented or approved. | Automated tests cannot claim universal real-time refresh until product chooses watcher/polling/activation/manual semantics and stale surfaces are inventoried. Substitute verification is targeted refresh tests plus explicit documentation that broad watcher/polling is absent. |
| DBG-BET-001 | `tests/test_tui_entrypoint.py::test_tui_startup_metadata_covers_all_primary_views_and_shortcuts` proves the menu shortcut appears in startup metadata and numeric view labels are not bindings. `test_tui_menu_binding_opens_view_submenu_and_theme_entry` proves uppercase `M` opens the menu, exposes secondary actions, opens the view submenu, and switches views. Step 4 adds `test_tui_upper_left_menu_button_opens_menu` for the clickable `#menu-button`. | Coverage is behavior-level for keyboard menu hub, clickable upper-left affordance, and secondary-action relocation. | Future visual placement or styling changes still need screenshot/preview approval if user requests a different look. |
| DBG-BET-002 | `tests/test_tui_entrypoint.py::test_prompt_input_treats_m_and_shift_m_as_menu_key`, `test_prompt_input_treats_lowercase_m_as_ordinary_text`, and `test_prompt_input_keeps_removed_shortcuts_and_ordinary_keys_as_text` prove uppercase/Shift `M` is a menu key while lowercase `m`, `p`, digits, `f`, and `?` remain ordinary input. | Coverage is behavior-level for Textual key classification and input preservation. No new test added. | OS-level IME composition behavior cannot be fully proven in headless unit tests; substitute verification is Textual key/input tests plus manual IME checks on Windows/macOS/Linux when shortcut policy changes. |
| DBG-BET-003 | Repository-level tests such as `tests/test_repo_hygiene.py`, `tests/test_regression_baseline.py`, release smoke/validation tests, and broad module tests provide regression gates around existing ownership and public surfaces. | No new test added because this item is an audit/refactor program, not a concrete behavior change. | Automated tests cannot prove “every function is correctly positioned and optimized.” Substitute verification is architecture/function-map review plus scoped refactor tasks with pre/post behavior tests and approval. |
| DBG-BET-004 | Current TUI regression tests exercise layout-adjacent behavior (`tests/test_tui_entrypoint.py`, `tests/test_tui.py`, `tests/test_tui.py`, `tests/test_tui.py`, `tests/test_tui.py`). | No new test added because the required preview image/approval artifact is external and no approved visual redesign was implemented. | Automated tests cannot prove user approval of a preview saved to `C:\Users\D2O\Downloads`; substitute verification is producing the preview artifact, obtaining user confirmation, then adding focused widget/style tests for any approved implementation. |
| DBG-BET-005 | Existing repo/doc hygiene tests read release documentation, and README wordmark presence is repository-visible. | No new test added because the implemented minimal text logo is documentation/branding, not runtime behavior, and adding a doc-text-only assertion would be superficial. | Final logo taste/branding acceptance requires user approval. Substitute verification is Markdown review and release-doc packaging checks. |
| DBG-BET-006 | `tests/test_tui_entrypoint.py::test_readme_tui_docs_match_bindings_and_preserve_boundaries`, `tests/test_repo_hygiene.py`, and `tests/test_beta042_release_validation.py` provide executable documentation consistency and release-surface anchors. | Coverage is partially behavior-linked where docs must match actual TUI bindings and boundaries. No new doc-only language split test added in this pass. | Professional editorial quality across every Markdown file and remote Wiki cannot be fully proven by tests; substitute verification is human docs review plus focused consistency tests for commands/bindings/packaging. |
| DBG-BET-007 | `tests/test_tui_entrypoint.py::test_tui_labels_follow_chinese_first_policy_with_reasonable_english_terms` proves retained English emphasis terms (`User`, `System`, `Assistant`, `Output`, etc.) coexist with the current Chinese-first localization policy; `tests/test_tui.py` proves chat labels render through localized label keys. | Coverage is behavior-level for current label rendering and localization policy. No new test added because no approved full English single-word sweep exists. | Full single-word inventory conflicts with preserved Chinese localization and needs product decision; substitute verification is string inventory and approval before a style-enforcement test. |
| DBG-BET-008 | `tests/test_tui.py::test_chat_submit_sets_processing_state_locks_main_input_and_displays_status`, `test_chat_processing_locks_only_main_prompt_input`, `test_chat_repeated_submit_while_processing_ignores_second_worker`, `test_chat_processing_unlocks_on_completion`, `test_chat_processing_status_refreshes_when_switching_views`, and `test_chat_processing_unlocks_on_exception` prove dynamic Processing state, duplicate-submit rejection, prompt locking scope, and unlock on success/exception. | Coverage is behavior-level and strong for the requested chat status checker. No new test added. | Cancellation UX remains unimplemented and would need separate tests only if designed. |
| DBG-BET-009 | `tests/test_tui_entrypoint.py::test_tui_startup_metadata_covers_all_primary_views_and_shortcuts`, `test_readme_tui_docs_match_bindings_and_preserve_boundaries`, `test_tui_menu_binding_opens_view_submenu_and_theme_entry`, and ordinary-input shortcut tests prove global shortcuts are reduced to essential uppercase actions and secondary actions live under the `M` menu. | Coverage is behavior-level for binding inventory, menu relocation, and ordinary input preservation. No new test added. | Any future shortcut expansion must update tests/docs/menu together. |
| DBG-BET-010 | Repository docs are exercised by existing doc/release tests, but no test can prove remote Wiki publication. | No test added because a doc-text assertion would not prove GitHub Wiki content exists at the remote URL. | Remote Wiki publication is external/approval-gated. Substitute verification is an authorized Wiki workflow (`wiki.git` clone/push or equivalent), page list/content review, and remote URL inspection after Step 4 publishing. |
| DBG-Q-001 | `tests/test_orchestrator.py`, `tests/test_opencode_adapter.py::TestOpenCodeRealDispatch`, adapter degraded-boundary tests, `tests/test_integration.py`, and `tests/test_kernel_full.py` prove orchestrator dispatch works when explicitly used, normal Kernel execution works, and the OpenCode adapter degrades without an injected orchestrator. | Coverage is behavior-level for the diagnostic conclusion: components work, but normal chat is not automatically multi-agent. No new test added. | If product wants multi-Agent as default chat routing, that is a new mode/UX change requiring dedicated tests for the selected dispatch path. |
| DBG-REB-001 | `tests/test_opencode_adapter.py` and `tests/test_repo_hygiene.py` prove current optional OpenCode adapter boundaries, degraded native runtime claims, permission-related capabilities, and install metadata. | Coverage is behavior-level for current safe adapter claims, not full external alignment. No new test added. | Automated local tests cannot prove a fresh full comparison against `https://github.com/anomalyco/opencode` without performing a legal-safe external audit. Substitute verification is a comparison matrix and approval before alignment tasks. |
| DBG-REB-002 | Broad regression suites provide future safety gates for behavior-preserving refactors, and architecture docs record no-delete/no-rename guardrails. | No new test added because no duplicate merge, dead-code deletion, or function-count reduction was performed. | Automated tests cannot prove a broad refactor that has not been specified or approved. Substitute verification is exact duplicate/dead-code evidence, scoped refactor plans, targeted tests, and broad Tester regression after each approved change. |

### Step 3 Tests Changed / Gaps

- **Tests changed in Step 3:** none. The audit found existing executable behavior coverage for implemented/local behaviors and avoided superficial tests that only assert documentation text.
- **Executable coverage accepted without changes:** permission mode/root normalization, session log aggregation, Python/R tool scan/import, safe Thinking/progress display, targeted refresh, `M` menu behavior, uppercase-only shortcut classification, Chat Processing lock, shortcut reduction/menu relocation, multi-agent diagnostic boundaries, and OpenCode adapter boundary claims.
- **Manual/external/approval-gated gaps intentionally carried forward after Step 4:** Wiki remote availability/content remains externally verified even though publication evidence is now recorded as commit `d6a1e11`; TUI preview user approval and image/screenshot rendering remain unrecorded; OS-level IME proof, full English-only TUI title sweep under Chinese-first localization constraints, full external Opencode comparison/alignment, and broad whole-repo refactor/optimization remain gated.
- **No validation command run in this Coder pass:** Tester owns execution verification for the cited tests.

## Step 5 Documentation Alignment Pass

**Review date:** 2026-06-08
**Purpose:** 核对 Step 4 后 README、中文 README 与本台账/计划的描述，确保文档只描述已有代码、测试和已记录外部证据，不把审批或外部验证事项误报完成。

| Documentation target | Step 5 alignment result |
| --- | --- |
| Upper-left menu affordance | README and README.zh-CN now state that the main menu opens through uppercase `M` or the upper-left sidebar `≡ 菜单 (M)` affordance, matching `core/tui/app.py::MenuButton` / `#menu-button` evidence from Step 4. |
| Uppercase `M`/`P`/`Q` policy | README entries continue to document uppercase-only alphabetic global shortcuts and lowercase/IME text preservation; Step 5 keeps the policy scoped to documented Textual key/input behavior and does not claim OS-level IME proof. |
| Targeted dynamic refresh inventory | README and README.zh-CN now name the code-backed targeted refresh inventory as workspace, log, dashboard, tool, and dialog surfaces and explicitly state that no broad watcher or background polling loop is implemented. |
| TUI preview artifact workflow | README and README.zh-CN now describe `scripts/tui_preview_artifact.py` as a text preview artifact workflow that defaults to Downloads, does not generate an image by itself, and records no user approval claim. Full visual redesign remains approval-gated. |
| Wiki publication evidence | README and README.zh-CN now reference the architecture tracking evidence for remote GitHub Wiki commit `d6a1e11` while preserving the boundary that local tests cannot prove remote Wiki availability or future content. |
| TUI title/string inventory boundary | README and README.zh-CN now describe the English emphasis-label inventory (`User`, `System`, `Assistant`, `Error`, `Status`, `Output`) and the retained Chinese-first navigation/screen-title boundary, avoiding an unsupported full English-only title sweep claim. |
| Remaining external/approval-gated items | README and README.zh-CN now list remaining gated work: TUI preview approval, image/screenshot preview output if specifically required, OS-level IME proof, full English-only title replacement under Chinese localization constraints, full external OpenCode comparison/alignment, and broad whole-repo refactor. |

Step 5 changed documentation only. It did not modify source code, tests, remote Wiki content, generated artifacts, or approval state.

## Step 6 Final Implementation and CI Closure Summary

**Review date:** 2026-06-08
**Purpose:** 汇总 `Debug.txt` 18 项落实对照、CI 失败根因与修复、验证证据来源、以及 artifact 处理状态。本节只引用既有源码/测试/文档定位与 Tester 已完成的验证结果；Step 6 未运行会生成 artifacts 的验证命令。

### Debug.txt 18 项最终对照

| ID | Final status | Evidence-backed result |
| --- | --- | --- |
| DBG-BUG-001 | 已修复/已落实 | 权限确认输入与聊天输入分离：`core/tui/screens/permission_screen.py` 使用 `#permission-confirm-input` 处理 `FULL` 确认，`core/tui/app.py` 只把 `#prompt-input` 提交路由到聊天；授权目录带引号/相对路径归一化由 `core/config_center.py` 实现；覆盖证据为 `tests/test_tui.py` 与 `tests/test_config_center.py::test_file_access_authorized_roots_normalize_quoted_absolute_and_relative_paths`。 |
| DBG-BUG-002 | 已落实 | 单 session 日志容器已存在：`core/log_report.py` 定义 `TUI_LOG_SESSION_ID = "tui-application"` 并写入 `session-<session_id>.json`；`core/tui/app.py` 启动前配置 TUI log storage；覆盖证据为 `tests/test_log_report.py::test_session_writes_append_to_one_redacted_log`。 |
| DBG-BUG-003 | 已落实 | Python/R 工具扫描与 heatmap/UMAP 模板由 `core/workspace_tools.py` 的语言/manifest 扫描和 `BUILTIN_TEMPLATES` 支撑；manifest 证据包括 `plugins/tools/python_data_analysis/tool.yaml` 与 `plugins/tools/r_data_analysis/tool.yaml`；覆盖证据为 `tests/test_workspace_full.py`。 |
| DBG-BUG-004 | 已落实（安全 Thinking/进度展示） | `core/kernel.py::execute_task()` 接受 `progress_callback`，`core/tui/app.py::_run_kernel_task()` 渲染 `reasoning`/`status`/streaming 事件，`core/tui/screens/chat_view.py::add_reasoning_status()` 展示安全进度；覆盖证据为 `tests/test_tui.py`。 |
| DBG-BUG-005 | 已落实（targeted refresh） | `core/tui/app.py::_refresh_visible_dynamic_data()` 在视图激活时调用 refresh hook；`SuperMedicineTUI.dynamic_refresh_surfaces()` 记录 workspace/log/dashboard/tool/dialog 的 targeted refresh 清单和 no watcher/no polling 边界；`core/tui/screens/workspace_screen.py::refresh_view_data()` 和 `#workspace-refresh` 手动刷新读取外部变更；CI 回归覆盖为 `tests/test_tui.py::_wait_for_tui_condition` 与 `test_workspace_view_refresh_button_reads_external_workspace_created_after_enter`。 |
| DBG-BET-001 | 已落实 | `core/tui/app.py` 提供 `MainMenuScreen`、`ViewSelectMenuScreen`、`action_open_menu()` 与菜单入口；新增 `MenuButton` / `#menu-button` 在左上侧边栏显示 `≡ 菜单 (M)` 并可点击打开菜单；`core/tui/i18n.py` 提供菜单本地化；README/中文 README 的 TUI 章节记录 `M` 菜单。 |
| DBG-BET-002 | 已修复/已落实 | uppercase-only 全局字母快捷键由 `core/tui/app.py` 的 `Binding("M")`/`Binding("P")`/`Binding("Q")` 与 PromptInput 处理实现；覆盖证据为 `tests/test_tui_entrypoint.py` 中 uppercase 绑定、Shift/uppercase `M`、lowercase 普通输入用例。 |
| DBG-BET-003 | 已落实为审计/无需重构 | 仓库职责与边界由 `FUNCTION_MAP.md`、`ARCHITECTURE.md`、`Architecture/MaintainerRepositoryReading.md`、`Architecture/RepositoryOptimizationAudit.md` 记录；未进行未经批准的结构重构。 |
| DBG-BET-004 | 部分落实/需用户确认 | Opencode-like minimal TUI 方向已有 `core/tui/app.py` 与 `core/tui/app.tcss` 实现基础；新增 `scripts/tui_preview_artifact.py` 可默认向 `C:\Users\D2O\Downloads` 写入文本预览 artifact（无须图像渲染），并明确不记录/不声称用户批准。 |
| DBG-BET-005 | 已落实 | `README.md` 与 `README.zh-CN.md` 顶部嵌入 minimal text logo/wordmark，未引入二进制重资产。 |
| DBG-BET-006 | 已落实 | `README.md` 为英文入口，`README.zh-CN.md` 为中文入口；两者分别包含安装、TUI、权限、工具、日志、质量门等 release 文档内容。 |
| DBG-BET-007 | 已落实（英文重点标签库存约束/本地化保留） | `core/tui/i18n.py::tui_title_style_inventory()` 对英文重点标签 single capitalized word 进行代码库存约束（如 `User`、`System`、`Assistant`、`Error`、`Status`、`Output`），同时保留 Chinese-first 导航/标题；不误报为完全英文样式替换。 |
| DBG-BET-008 | 已落实 | `core/tui/app.py` 使用 `_chat_processing`、`_begin_chat_processing()`、输入禁用/恢复和 duplicate-submit 拒绝；`core/tui/i18n.py` 提供 `Chat Processing` 文案；覆盖证据为 `tests/test_tui.py` 与 `tests/test_tui_entrypoint.py` README 文档断言。 |
| DBG-BET-009 | 已修复/已落实 | `core/tui/app.py` 全局绑定限制为 uppercase `M`/`P`/`Q` 关键动作，次级动作进入菜单；覆盖证据为 `tests/test_tui_entrypoint.py` 的绑定/普通输入回归。 |
| DBG-BET-010 | 已发布证据已记录 | 可作为 Wiki 来源的专业文档已存在于 `README.md`、`README.zh-CN.md`、`ARCHITECTURE.md`、`INSTALL.md` 与 Architecture 审计文档；远程 GitHub Wiki 发布证据记录为 commit `d6a1e11`。本仓库不把本地测试当作远程 Wiki 可用性证明。 |
| DBG-Q-001 | 已核查/无需修改 | `agents/orchestrator.py` 提供 in-process orchestration；普通 CLI/TUI 聊天路径仍由 `core/kernel.py::execute_task()` 执行；`adapters/opencode/adapter.py` 只有注入 orchestrator 时才分发，否则为 metadata fallback；`Architecture/PlatformIntegrationAudit.md`/README 记录该边界。 |
| DBG-REB-001 | 已落实为安全对齐审计/无需复制外部源码 | Opencode 对齐边界记录在 `ARCHITECTURE.md` 与 `Architecture/PlatformIntegrationAudit.md`；README 将 OpenCode 描述为 optional adapter surface；本轮未抓取或复制外部源码。 |
| DBG-REB-002 | 已落实为审计/无需重构 | `Architecture/RepositoryOptimizationAudit.md` 与 `FUNCTION_MAP.md` 提供重复逻辑/死代码/行为保持边界；没有在缺少证据和批准时进行全仓库重构或删除。 |

### CI root cause and repair summary

| Area | Evidence / result |
| --- | --- |
| CI failure observed from LOG.zip | 本文 `CI Log Evidence from LOG.zip` 表记录：Windows Python 3.10/3.11/3.12 `Release gate` 在 `tests/test_tui.py::test_workspace_view_refresh_button_reads_external_workspace_created_after_enter` 失败，`DataTable(id='workspace-table').row_count` 在点击 `#workspace-refresh` 后仍为 `0`。 |
| Root cause | 失败路径不是业务数据创建失败，而是 Windows/Textual 异步 TUI 刷新后立即断言造成的 race：`WorkspaceManager(tmp_path).initialize_workspace("external-a")` 已创建外部 workspace，但测试需要等待 UI 处理点击和表格重绘后再读取 `row_count`。 |
| Repair | `tests/test_tui.py::_wait_for_tui_condition` 引入 condition-based wait；同文件 `test_workspace_view_refresh_button_reads_external_workspace_created_after_enter` 在点击 `#workspace-refresh` 后等待 `table.row_count == 1`，再断言 `external-a` 行与刷新状态。 |
| Source behavior involved | `core/tui/screens/workspace_screen.py` 的 `on_button_pressed(... #workspace-refresh ...)` 调用 `_load_workspaces(refreshed=True)`；`_load_workspaces()` 清表、读取 controller workspace 列表并添加 table row。该行为与测试等待目标一致。 |

### Local/Tester verification evidence

| Verification source | Result summary |
| --- | --- |
| Tester-owned validation | Prior Tester verification for the preceding implementation/coverage steps is the accepted verification source for this Step 6 documentation closure. Step 6 did not rerun test commands because the task explicitly forbids artifact-generating validation commands and allows citing Tester-passed results. |
| Covered commands by plan | `Architecture/DebugBugFixPlan.md` lists the targeted acceptance commands for permission, tools, logs, LLM thinking, multi-agent investigation, and refresh areas. The Step 4 section above maps the final added/confirmed coverage to `tests/test_config_center.py`, `tests/test_tui_entrypoint.py`, and `tests/test_tui.py`. |
| Step 6 command policy | No `pytest`, build, wheel, coverage, or other artifact-producing command was run in Step 6. Documentation updates only cite code/test locations and prior Tester verification. |

### Artifact cleanup status

| Artifact area | Result |
| --- | --- |
| Generated validation artifacts | Step 6 did not generate new test/build/coverage artifacts because no artifact-generating validation command was executed. |
| Repository cleanup boundary | Existing cleanup rules remain those documented in `Architecture/MaintainerRepositoryReading.md`: generated/cache/build/binary/runtime/local-only artifacts stay excluded or require explicit confirmation when ambiguous; tracked tests, CI, docs, architecture reports, and source are protected. |
| Files changed by Step 6 | Only tracking documents in `Architecture/DebugReviewTaskLedger.md` and `Architecture/DebugBugFixPlan.md` were updated for final closure; no source/runtime artifact deletion or creation is claimed here. |

## Step 7 Artifact Generation and Content Relevance Check

**Review date:** 2026-06-08
**Purpose:** 核对 `Debug.txt` 相关 artifact 生成、artifact 内容边界、package/build 相关性，以及 Step 4 新增 TUI preview artifact workflow。此步骤生成外部 Downloads deliverable，但不把它作为仓库文件提交。

| Check target | Step 7 evidence / result |
| --- | --- |
| TUI preview artifact generation | Executed `python scripts/tui_preview_artifact.py --project-root D:\GIT\SuperMedicine`; generated `C:\Users\D2O\Downloads\SuperMedicine_TUI_preview.txt`. The artifact is intentionally outside the repository and must not be deleted by repo cleanup. |
| Preview content relevance | The generated text artifact records dry-run TUI shell metadata (title, current view, shortcut hint, status fields), the upper-left `≡ 菜单 (M)` affordance, uppercase `M` fallback, and the targeted refresh inventory for workspace/log/dashboard/tool/dialog. This maps to Debug.txt TUI/menu/shortcut/refresh/preview items without claiming a visual redesign is approved. |
| Approval/image boundary | The generated artifact explicitly states that user approval has **NOT** been recorded and says image rendering requires a separate terminal screenshot capture. It is a bounded text preview artifact, not an image artifact and not user approval evidence. |
| Package/build relevance | `pyproject.toml` exposes only the package console script `supermedicine = "Cli:main"` and setuptools package data for runtime packages/assets. `setup.py` customizes installer wrapper inclusion and stale distribution member removal. `scripts/tui_preview_artifact.py` is a repository utility script, not an installed package command and not included in the setuptools package discovery; therefore no package script change is required unless the product later decides the preview generator should become a public CLI. |
| Repository artifact cleanup | No `build/`, `dist/`, `*.egg-info`, `.pytest_cache`, `.mypy_cache`, `.ruff_cache`, `__pycache__`, or `*.pyc` artifacts were present during this pass. Runtime/local `.supermedicine/` artifacts were present and cleaned from the repository working tree after artifact generation. |

Step 7 does not modify `scripts/tui_preview_artifact.py` or package configuration. It only records evidence and cleanup status in tracking docs while preserving the generated Downloads preview deliverable.

## Step 10 Final 18-Item Implementation Report

**Review date:** 2026-06-08
**Purpose:** 汇总 Step 1-9 已验证证据，形成 `Debug.txt` 18 项最终落实报告。此节只记录证据和边界；不运行 artifact-generating 命令、不提交/推送主仓库、不声称 GitHub Actions CI 成功。

### Step 10 Global Validation and Publication Evidence

| Evidence area | Final Step 10 record |
| --- | --- |
| Local validation | Latest full local validation evidence: `ruff` **PASS**, `mypy` **PASS**, build **PASS**, `pytest` **859 passed, 4 skipped**. This is the local validation result used for closure. |
| Remote validation | GitHub Wiki remote pages are accessible at `https://github.com/KarasawaYikiho/SuperMedicine/wiki`; latest Wiki commit evidence is `e5172dc1cdd7dc328a61f117f6d309a000d32771`. |
| Remote CI boundary | GitHub Actions CI status was **not queried** because `gh` is unavailable. Local validation above is substitute evidence only; this report makes no remote CI success claim. |
| External artifact | TUI preview artifact evidence is `C:\Users\D2O\Downloads\SuperMedicine_TUI_preview.txt`. It is text-only, records no user approval, and is not an image/screenshot approval artifact. |
| Main repository state | Main repository changes remain local/uncommitted unless separately requested. This report does not commit or push main repo changes. |

### Step 10 Final Matrix

| ID | Final implementation report |
| --- | --- |
| DBG-BUG-001 | **Original requirement:** Permission `P`/`FULL` switching must work, `FULL` must not leak to chat, conservative authorized directories must accept absolute/relative and quoted/unquoted paths, and the TUI permission indicator must be integrated. **Code evidence:** `core/tui/screens/permission_screen.py::PermissionScreenController.set_mode()` and `#permission-confirm-input`; `core/tui/app.py::SuperMedicineTUI.on_input_submitted()` only routes `#prompt-input` to chat; `core/config_center.py::_normalize_user_directory_path()`, authorize/revoke methods, and `get_file_access_policy()` normalize and apply roots; `_permission_status_label()` and sidebar permission entry surface status. **Executable/manual evidence:** `tests/test_tui.py`, `tests/test_config_center.py::test_file_access_authorized_roots_normalize_quoted_absolute_and_relative_paths`, plus permission/policy/path tests cited in Step 1-4; latest full local validation is ruff/mypy/build PASS and pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN permission sections and this ledger/`Architecture/DebugBugFixPlan.md` record the behavior. **Wiki/remote evidence:** latest Wiki commit `e5172dc1cdd7dc328a61f117f6d309a000d32771` with remote pages accessible; no remote CI claim because `gh` unavailable. **Artifact evidence:** no separate artifact required. **Local result:** implemented and locally validated. **Remote result:** Wiki documents are remotely accessible; CI unqueried. **Remaining risks/boundaries:** final visual preference for exact permission indicator placement remains a UI approval matter if user requests a different layout. |
| DBG-BUG-002 | **Original requirement:** Logs should aggregate per application opening/session with all logs before exit in one session container. **Code evidence:** `core/log_report.py` defines `TUI_LOG_SESSION_ID`, `resolve_log_storage_locations()`, and session JSON append semantics; `core/tui/app.py::launch_tui()` calls `configure_tui_log_storage()`; stream capture routes stdout/stderr through log storage; `core/tui/screens/log_screen.py` reads/follows log records. **Executable/manual evidence:** `tests/test_log_report.py::test_session_writes_append_to_one_redacted_log` and `tests/test_tui.py`; latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN log sections and DebugBugFixPlan Step notes. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** runtime `.supermedicine/logs/session-<id>.json` is local/redacted and not a committed artifact. **Local result:** implemented as stable session container. **Remote result:** docs published to Wiki; no remote CI claim. **Remaining risks/boundaries:** if user later defines “one log” as a unique physical file per launch instead of the stable session file, semantics require a new scoped change. |
| DBG-BUG-003 | **Original requirement:** R/Python tools, including heatmap and UMAP, should be recognized, scanned, and imported. **Code evidence:** `core/workspace_tools.py` validates Python/R, defines `BUILTIN_TEMPLATES` for Python/R heatmap/UMAP, scans `plugins/tools`, accepts `tool.yaml` and `plugin.yaml`, and imports selected candidates; manifests exist under `plugins/tools/python_data_analysis/tool.yaml`, `plugins/tools/r_data_analysis/tool.yaml`, `plugins/tools/r_template/tool.yaml`, `plugins/tools/python_stats/plugin.yaml`, and `plugins/tools/r_survival/plugin.yaml`; TUI handoff exists in `core/tui/screens/tool_screen.py`. **Executable/manual evidence:** `tests/test_workspace_full.py`, workspace CLI/plugin tests, latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN tool sections and plugin tool READMEs. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** tool manifests/runners are source artifacts; workspace imports are local runtime copies. **Local result:** implemented and validated. **Remote result:** docs published; no CI success claim. **Remaining risks/boundaries:** user-provided tools still require concrete manifest/scan-output evidence before any targeted repair. |
| DBG-BUG-004 | **Original requirement:** Dialog must show visible dynamic Thinking/progress updates during LLM work. **Code evidence:** `core/kernel.py::Kernel.execute_task(... progress_callback=...)` emits `reasoning`, `status`, `assistant_start`, and `assistant_delta`; `core/tui/app.py::_run_kernel_task()` renders those events; `core/tui/screens/chat_view.py::add_reasoning_status()` and streaming methods display updates. **Executable/manual evidence:** `tests/test_tui.py`, adjacent kernel/LLM tests, latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN TUI chat sections and DebugBugFixPlan Thinking/progress boundary. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** no separate artifact required. **Local result:** implemented as provider-safe progress/Thinking surface. **Remote result:** docs published; no remote CI claim. **Remaining risks/boundaries:** raw hidden chain-of-thought is intentionally not exposed; any raw provider reasoning display requires explicit policy/provider approval. |
| DBG-BUG-005 | **Original requirement:** Dynamically changing data/files must refresh in real time. **Code evidence:** `core/tui/app.py::_refresh_visible_dynamic_data()` and `SuperMedicineTUI.dynamic_refresh_surfaces()` provide targeted activation/manual refresh inventory; workspace/log/dashboard/tool/dialog surfaces expose refresh hooks/buttons such as `core/tui/screens/workspace_screen.py::refresh_view_data()` and `#workspace-refresh`. **Executable/manual evidence:** `tests/test_tui.py::test_workspace_view_refresh_button_reads_external_workspace_created_after_enter` with condition wait, `tests/test_tui.py`, `tests/test_tui.py`, latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN targeted refresh sections explicitly state no broad watcher/polling loop. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** no separate artifact required. **Local result:** implemented under targeted refresh policy. **Remote result:** docs published; no CI success claim. **Remaining risks/boundaries:** universal filesystem watching/periodic polling is not implemented; new stale surfaces require inventory and confirmed real-time policy. |
| DBG-BET-001 | **Original requirement:** Settings should be unified under `M`, reachable from an upper-left/menu affordance, with complete localization. **Code evidence:** `core/tui/app.py::MainMenuScreen`, `ViewSelectMenuScreen`, `action_open_menu()`, uppercase `Binding("M")`, `MenuButton` / `#menu-button` with `≡ 菜单 (M)`, and `core/tui/i18n.py` menu strings. **Executable/manual evidence:** `tests/test_tui_entrypoint.py::test_tui_menu_binding_opens_view_submenu_and_theme_entry` and `test_tui_upper_left_menu_button_opens_menu`; latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN TUI shortcut/menu docs. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** preview text artifact references the menu affordance at `C:\Users\D2O\Downloads\SuperMedicine_TUI_preview.txt`. **Local result:** implemented. **Remote result:** docs published; no remote CI claim. **Remaining risks/boundaries:** further visual polish remains preview/approval-gated. |
| DBG-BET-002 | **Original requirement:** Shortcuts should be uppercase-only, work outside chat focus, and preserve input-method compatibility. **Code evidence:** uppercase global `Binding("M")`, `Binding("P")`, `Binding("Q")` in `core/tui/app.py`; `PromptInput` preserves lowercase/ordinary/IME-like text and filters terminal controls. **Executable/manual evidence:** `tests/test_tui_entrypoint.py` uppercase/Shift/lowercase/ordinary-input coverage; latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN uppercase shortcut policy. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** preview text artifact notes uppercase shortcuts. **Local result:** implemented at Textual key/input layer. **Remote result:** docs published; no CI success claim. **Remaining risks/boundaries:** OS-level IME composition proof remains manual/platform-specific. |
| DBG-BET-003 | **Original requirement:** Deeply analyze functions/structure so every function is correctly positioned and optimized. **Code evidence:** repository ownership remains in `core/**`, `permission/**`, `agents/**`, `plugins/**`, `adapters/**`; no broad relocation was made. **Executable/manual evidence:** broad regression tests and latest full local validation PASS with pytest `859 passed, 4 skipped` protect existing behavior. **Docs evidence:** `FUNCTION_MAP.md`, `ARCHITECTURE.md`, `Architecture/MaintainerRepositoryReading.md`, and `Architecture/RepositoryOptimizationAudit.md`. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** no artifact required. **Local result:** completed as audit/guardrail documentation, not a hidden refactor. **Remote result:** docs published; no remote CI claim. **Remaining risks/boundaries:** “every function optimized” is not claimed; any restructuring requires exact evidence, tests, and approval. |
| DBG-BET-004 | **Original requirement:** Beautify TUI, first output preview to `C:\Users\D2O\Downloads`, get correction/confirmation, then implement. **Code evidence:** current minimal TUI style in `core/tui/app.py` theme and `core/tui/app.tcss`; `scripts/tui_preview_artifact.py` provides text preview workflow. **Executable/manual evidence:** `tests/test_tui_entrypoint.py::test_tui_preview_artifact_workflow_writes_text_without_approval_claim`; latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN preview-artifact wording and ledger Step 7. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** external artifact exists at `C:\Users\D2O\Downloads\SuperMedicine_TUI_preview.txt`; it is text-only, not image output, and explicitly records no user approval. **Local result:** preview workflow/artifact evidence complete; full beautification is not claimed. **Remote result:** docs published; no CI success claim. **Remaining risks/boundaries:** image/screenshot preview and user-approved visual redesign remain gated. |
| DBG-BET-005 | **Original requirement:** Design a theme-appropriate logo and embed it into Markdown. **Code evidence:** no runtime code required. **Executable/manual evidence:** doc/release hygiene coverage plus latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** `README.md` and `README.zh-CN.md` include centered minimal text wordmark. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** no binary logo artifact; intentionally text-only Markdown. **Local result:** implemented as minimal text logo baseline. **Remote result:** docs published; no remote CI claim. **Remaining risks/boundaries:** alternate visual branding requires user concept approval. |
| DBG-BET-006 | **Original requirement:** Markdown docs must be professional, release-grade, and language-separated. **Code evidence:** no runtime code required. **Executable/manual evidence:** `tests/test_repo_hygiene.py`, `tests/test_beta042_release_validation.py`, README/TUI consistency tests, latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** `README.md` is English entry, `README.zh-CN.md` is Chinese entry, with install/TUI/permission/tools/logs/quality content; `INSTALL.md`, `ARCHITECTURE.md`, and `FUNCTION_MAP.md` provide supporting docs. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** release docs are source artifacts; no generated artifact required. **Local result:** implemented for main README language split. **Remote result:** docs published; no CI success claim. **Remaining risks/boundaries:** exhaustive editorial review of every Markdown page and future Wiki edits remains a human docs task. |
| DBG-BET-007 | **Original requirement:** TUI titles/emphasis should use single words with initial capitalization. **Code evidence:** `core/tui/i18n.py::tui_title_style_inventory()`, `TUI_ENGLISH_TITLE_STYLE_KEYS`, and retained labels `User`, `System`, `Assistant`, `Error`, `Status`, `Output`; Chinese-first navigation titles remain localized. **Executable/manual evidence:** `tests/test_tui_entrypoint.py::test_tui_title_style_inventory_enforces_english_emphasis_without_replacing_chinese` and chat-view label tests; latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN explain English emphasis inventory and Chinese localization boundary. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** no separate artifact required. **Local result:** implemented for enforceable English emphasis labels. **Remote result:** docs published; no remote CI claim. **Remaining risks/boundaries:** full English-only conversion is not claimed and needs product/localization approval. |
| DBG-BET-008 | **Original requirement:** Add Chat status checker showing dynamic `Processing` while dialog is running and lock duplicate input. **Code evidence:** `core/tui/app.py` `_chat_processing`, `_begin_chat_processing()`, `_set_chat_processing()`, prompt disable/restore, duplicate rejection, and finally-unlock; `core/tui/i18n.py` maps `Chat Processing`; `core/tui/screens/chat_view.py` renders status. **Executable/manual evidence:** `tests/test_tui.py` processing lock/reject/unlock/status/exception tests and latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN chat sections. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** no separate artifact required. **Local result:** implemented and validated. **Remote result:** docs published; no CI success claim. **Remaining risks/boundaries:** cancellation UX is separate future design if requested. |
| DBG-BET-009 | **Original requirement:** Reduce unnecessary shortcuts; retain important shortcuts such as `P` and `M`, move reduced actions into `M` menu. **Code evidence:** `core/tui/app.py::SuperMedicineTUI.BINDINGS` limited to uppercase `Q`, `M`, `P`; `MainMenuScreen` holds secondary actions. **Executable/manual evidence:** `tests/test_tui_entrypoint.py` binding/menu/ordinary-input coverage and latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README/README.zh-CN shortcut and menu docs. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** preview text artifact references shortcut/menu state. **Local result:** implemented. **Remote result:** docs published; no remote CI claim. **Remaining risks/boundaries:** any future shortcut expansion must update code/docs/tests together. |
| DBG-BET-010 | **Original requirement:** Write professional, clearly organized content to GitHub Wiki. **Code evidence:** no runtime code required; source docs are `README.md`, `README.zh-CN.md`, `ARCHITECTURE.md`, `INSTALL.md`, `FUNCTION_MAP.md`, and Architecture audits. **Executable/manual evidence:** local doc/release tests plus latest full local validation PASS with pytest `859 passed, 4 skipped`; remote availability is manual/remote evidence, not a local test. **Docs evidence:** repo docs and tracking docs record Wiki publication. **Wiki/remote evidence:** remote Wiki pages are accessible; latest Wiki commit is `e5172dc1cdd7dc328a61f117f6d309a000d32771`, superseding earlier Step 4 commit references. **Artifact evidence:** remote Wiki itself is the external publication artifact. **Local result:** source docs and tracking updated. **Remote result:** Wiki accessible at latest commit; CI status not queried because `gh` unavailable. **Remaining risks/boundaries:** local tests cannot guarantee future remote Wiki availability or content after later edits. |
| DBG-Q-001 | **Original requirement:** Investigate whether multi-Agent works normally or normal dialogs always run single-Agent. **Code evidence:** `agents/orchestrator.py::Orchestrator.dispatch()` implements orchestration; normal TUI/CLI flows call `core/kernel.py::Kernel.execute_task()`; `adapters/opencode/adapter.py::OpenCodeAdapter.subagent_dispatch()` dispatches only when an orchestrator is injected, otherwise returns degraded metadata. **Executable/manual evidence:** `tests/test_orchestrator.py`, `tests/test_opencode_adapter.py`, `tests/test_integration.py`, `tests/test_kernel_full.py`, latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** README adapter table and `Architecture/PlatformIntegrationAudit.md`. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** no separate artifact required. **Local result:** diagnostic completed; normal chat is direct Kernel path, orchestrator exists for explicit/injected use. **Remote result:** docs published; no CI success claim. **Remaining risks/boundaries:** making multi-Agent default requires new UX/API/tests and user decision. |
| DBG-REB-001 | **Original requirement:** Deeply review external Opencode and align structure, TUI, permissions, Agent, and plugin mechanism. **Code evidence:** optional alignment surfaces in `adapters/opencode/*`, `core/tui/*`, `permission/*`, `agents/*`, and `plugins/*`; `OpenCodeAdapter.capabilities()` reports optional/degraded boundaries and no native OpenCode subagent runtime. **Executable/manual evidence:** `tests/test_opencode_adapter.py`, `tests/test_repo_hygiene.py`, integration/regression tests, latest full local validation PASS with pytest `859 passed, 4 skipped`. **Docs evidence:** `ARCHITECTURE.md`, `Architecture/PlatformIntegrationAudit.md`, README optional adapter docs. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** no external-source artifact committed. **Local result:** safe optional adapter/alignment boundary documented and validated; no external source copy. **Remote result:** docs published; no CI success claim. **Remaining risks/boundaries:** full fresh external Opencode comparison/alignment is not claimed and requires legal-safe audit plus approved tasks. |
| DBG-REB-002 | **Original requirement:** Deeply analyze whole repo and refactor logic while preserving behavior, reducing function count, efficiency, and maintainability. **Code evidence:** no broad refactor was executed; source remains behavior-preserving. **Executable/manual evidence:** broad local validation PASS (`ruff`, `mypy`, build, pytest `859 passed, 4 skipped`) provides regression baseline for future scoped refactors. **Docs evidence:** `Architecture/RepositoryOptimizationAudit.md`, `Architecture/MaintainerRepositoryReading.md`, `FUNCTION_MAP.md`, and `ARCHITECTURE.md` record duplicate/dead-code/refactor guardrails. **Wiki/remote evidence:** Wiki latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771`, remote pages accessible; CI not queried. **Artifact evidence:** no refactor artifact required. **Local result:** audit/guardrail closure only; no hidden function-count reduction or deletion. **Remote result:** docs published; no remote CI claim. **Remaining risks/boundaries:** future duplicate merge/dead-code deletion requires exact evidence, scoped plan, tests, and approval. |

### Step 10 Closure Boundaries

- This Step 10 report covers all 18 `Debug.txt` items exactly once and cross-references Step 1-9 code, executable/manual, docs, Wiki/remote, and artifact evidence where relevant.
- Local validation is recorded as the latest evidence (`ruff` PASS, `mypy` PASS, build PASS, `pytest 859 passed, 4 skipped`), but this Coder pass did not run validation commands.
- Remote Wiki evidence is updated to latest commit `e5172dc1cdd7dc328a61f117f6d309a000d32771` and accessible pages. Earlier `d6a1e11` notes remain historical Step 4 evidence, not the latest remote state.
- GitHub Actions CI status is not claimed because `gh` is unavailable and was not queried; local validation substitutes only for local confidence.
- `C:\Users\D2O\Downloads\SuperMedicine_TUI_preview.txt` is preserved as the external text preview artifact. It is not an image and does not record user approval.
- Main repository changes are local/uncommitted unless the user separately requests commit/push.
